:main_array_devices_help:
**Barevné označení stavu** Význam barevných ikon na začátku každého řádku u *disků pole (Array)* je následující:

<i class='fa fa-circle orb green-orb'></i>Normální provoz, disk je aktivní.

<i class='fa fa-circle orb grey-orb'></i>Disk je ve standby režimu (vypnutý).

<i class='fa fa-warning orb yellow-orb'></i>Obsah disku je emulován.

<i class='fa fa-times orb red-orb'></i>Disk je deaktivován, obsah je emulován.

<i class='fa fa-square orb blue-orb'></i>Nový disk.

<i class='fa fa-square orb grey-orb'></i>Není přítomen žádný disk, slot je prázdný.

**Identifikace** je *podpis*, který jednoznačně identifikuje disk. Obsahuje model, sériové číslo, identifikátor zařízení v Linuxu a velikost disku.

**Teplota** se čte přímo z disku. Zobrazené jednotky lze nastavit v [Nastavení zobrazení](Settings/DisplaySettings). Teploty se nezobrazují u vypnutých disků, protože by se tím probudily – místo toho se zobrazuje symbol "*". Tento symbol se rovněž zobrazuje u SSD a flash disků, které někdy vrací teplotu 0.

**Velikost, Využito, Volné** udávají celkovou velikost, využité a volné místo pro soubory. Jednotky lze také nastavit v [Nastavení zobrazení](Settings/DisplaySettings). Kvůli režii souborového systému není využité místo nikdy nulové, ani u prázdného disku.

*Poznámka: U cache poolu s více disky se tyto hodnoty vztahují k celému poolu podle výstupu z btrfs.*

**Čteno, Zapsáno** udává počet I/O požadavků odeslaných ovladačem na disk. Statistiky lze kdykoliv vynulovat – viz sekce o stavu pole níže.

**Chyby** uvádějí počet *neopravitelných* chyb hlášených ovladačem disku. Chybějící data jsou okamžitě rekonstruována pomocí parity (a je učiněn pokus o jejich zápis zpět do vadných sektorů). Každá neopravitelná chyba zápisu vede k *deaktivaci* disku.

**FS** ukazuje typ souborového systému detekovaný na první partition disku.

**Zobrazení** zobrazí ikonu složky, pokud je disk *připojen*. Kliknutím lze procházet jeho obsah.

Pokud je v [Nastavení zobrazení](Settings/DisplaySettings) aktivní volba „Zobrazit celkové hodnoty pole“, zobrazí se nový řádek **Celkem** s přehledem statistik disků včetně průměrné teploty.

Pole musí být zastaveno, pokud chceš změnit přiřazení disků v rámci pole.

UnRAID Array se skládá z jednoho nebo dvou paritních disků a libovolného počtu datových disků. Datové disky slouží výhradně k ukládání uživatelských dat, zatímco paritní disky poskytují potřebnou redundanci pro obnovu v případě selhání.

Protože nejsou data rozložena napříč polem, musí být paritní disky stejně velké nebo větší než největší datový disk. Paritní disk by měl být rovněž ten s nejvyšším výkonem.

Každý disk má vlastní souborový systém a může být exportován jako samostatné sdílené úložiště.

Kliknutím na název disku otevřeš jeho nastavení a nástroje.
:end

:main_slots_help:
**Sloty** Zvol počet diskových slotů ve svém serveru, které mají být použity pro disky pole (Array).
Minimální počet slotů pole je 2 a musíš přiřadit alespoň jeden disk do pole.
:end

:cache_devices_help:
**Barevné označení stavu** Význam barevných ikon na začátku každého řádku u *pool disků* je následující:

<i class='fa fa-circle orb green-orb'></i>Normální provoz, disk je aktivní.

<i class='fa fa-circle orb grey-orb'></i>Disk je ve standby režimu (vypnutý).

<i class='fa fa-square orb blue-orb'></i>Nový disk.

<i class='fa fa-square orb grey-orb'></i>Není přítomen žádný disk, slot je prázdný.

**Pool disky** jsou jeden nebo více disků *mimo* pole UnRAID. Mohou být exportovány pro síťový přístup stejně jako disky v poli. Díky tomu, že se nacházejí mimo pole, je zápis výrazně rychlejší.

Existují dvě možnosti konfigurace pool disků:

1. Jako jednotlivý disk, nebo  
2. Jako pool s více disky.

Pokud disk nakonfiguruješ jako jednotlivý, můžeš jej naformátovat kterýmkoli podporovaným souborovým systémem (btrfs, reiserfs nebo xfs). Tato konfigurace nabízí nejvyšší výkon, ale neposkytuje žádnou ochranu dat – při selhání disku budou všechna data ztracena.

Při konfiguraci jako pool s více disky Unraid OS automaticky použije formát *btrfs-raid1* (pro data i metadata). Btrfs umožňuje přidání libovolného počtu disků a zajišťuje, že každá položka bude uložena na dvou různých discích. Pool tak odolá výpadku jednoho disku.

Pokud jsou aktivní [uživatelské sdílené složky](/Settings/ShareSettings), lze je nakonfigurovat tak, aby automaticky používaly pool disky pro zvýšení rychlosti zápisu. Speciální proces na pozadí s názvem *Mover* může být nastaven tak, aby pravidelně přesouval soubory z cache na pole.
:end

:cache_slots_help:
**Sloty** Zvol počet diskových slotů ve svém serveru, které mají být použity pro pool disky (Cache/Pool).
:end

:boot_device_help:
Nejdůležitější konfigurace se ukládají na USB flash disk. Z tohoto důvodu musí zůstat připojený k serveru.
Klikni na [Flash](/Main/Flash?name=flash), kde zobrazíš GUID, registrační informace a můžeš upravit exportní nastavení.
Protože je USB flash disk naformátovaný jako FAT32, může být exportován pouze prostřednictvím SMB protokolu.
:end


:array_status_help:
**Barevné označení stavu** Význam barevného označení *pole (Array)* je následující:

<i class='fa fa-circle orb green-orb'></i>Pole je spuštěné a parita je platná.

<i class='fa fa-circle orb grey-orb'></i>Pole je zastavené a parita je platná.

<i class='fa fa-warning orb yellow-orb'></i>Pole je spuštěné, ale parita není platná.

<i class='fa fa-warning orb grey-orb'></i>Pole je zastavené a parita není platná.
:end


:array_devices_help:
#### Přiřazení disků

Pole disků UnRAID se skládá z libovolného počtu datových disků a až dvou paritních disků.  
Datové disky slouží výhradně k ukládání uživatelských dat, zatímco paritní disky poskytují nezbytnou redundanci pro obnovu při výpadku jednoho nebo dvou disků.

Proto je potřeba mít možnost přiřadit disky do slotů pole. To se provádí zde, na úvodní stránce, když je pole zastaveno.  
Pro každý disk pole je k dispozici rozbalovací seznam, ve kterém jsou uvedeny všechny dosud nepřiřazené disky.  
Chceš-li přiřadit disk, jednoduše jej vyber ze seznamu.  
Při každém přiřazení disku aktualizuje systém konfigurační soubor, aby tuto přiřazení uložil.

#### Požadavky

Na rozdíl od tradičních RAID systémů, kde jsou data rozložena napříč všemi disky pole, ukládá UnRAID soubory na jednotlivé disky.  
Při každém zápisu se tedy účastní jak cílový datový disk, tak i paritní disk(y). Z těchto důvodů:

* musí být paritní disk stejně velký nebo větší než kterýkoli z datových disků

a

* měl by být paritní disk (nebo disky) pokud možno nejrychlejšími disky v systému.

#### Doporučení

Níže jsou kroky, které bys měl(a) dodržet při plánování diskového pole v UnRAID:

1. Rozhodni se, který disk použiješ jako paritní a které disky budou datové (Disk 1, 2 atd.).  
   Označ si je a poznamenej si jejich sériová čísla — ty budeš potřebovat později.

2. Připoj disky, spusť server a otevři webové rozhraní. Pokud se jedná o nový systém, na úvodní stránce neuvidíš žádné disky,  
   což ale neznamená, že nebyly detekovány – pouze ještě nebyly přiřazeny.

3. Vzpomínáš si na sériová čísla z kroku 1? Pomocí nich nyní vyber správné disky pro paritu a datové pozice ze seznamu.

#### Hot Plug

Můžeš disky připojit i za běhu systému (*Hot Plug*), pokud to tvůj hardware podporuje.  
Například při použití diskových šuplíků můžeš disky zasunout do serveru za chodu, když je pole zastaveno.  
Aktualizuj stránku, aby se nově připojené nepřiřazené disky zobrazily v rozbalovacím seznamu.

#### Další kroky

Jakmile máš všechny disky přiřazeny, věnuj pozornost sekci „Stav pole“ níže a spusť pole.
:end


:encryption_help:
#### Šifrování

Když je pole zastaveno, lze nastavit nový šifrovací klíč. Upozorňujeme, že pokud byl disk již dříve zašifrován jiným klíčem,  
lze jej spustit pouze s tímto původním klíčem. Změna klíče u již zašifrovaných disků vyžaduje nové formátování,  
**což vede k trvalé ztrátě všech dat na těchto discích**.

#### Heslová fráze

Zadej heslovou frázi až do 512 znaků. Doporučuje se používat pouze 95 tisknutelných znaků z prvních 128 znaků  
[ASCII tabulky](https://en.wikipedia.org/wiki/ASCII), protože ty mají vždy stejný binární význam.  
Ostatní znaky mohou mít v různých systémech jinou kódovou reprezentaci, což způsobí, že heslová fráze nebude fungovat.

Pokud chceš použít delší frázi nebo zahrnout binární data, nahraj místo toho soubor s klíčem.

Pro pochopení toho, co tvoří *bezpečnou* heslovou frázi, si přečti [Cryptsetup-FAQ](https://gitlab.com/cryptsetup/cryptsetup/wikis/FrequentlyAskedQuestions#5-security-aspects).

**Zapamatuj si** svou heslovou frázi. **POKUD JI ZTRATÍŠ, ŠIFROVANÝ OBSAH NELZE OBNOVIT!**

#### Soubor s klíčem

Vyber lokální soubor s uloženým šifrovacím klíčem nebo binární soubor. Maximální velikost je 8 MB (8 388 608 bajtů).

**Zálohuj** svůj soubor s klíčem. **POKUD HO ZTRATÍŠ, ŠIFROVANÝ OBSAH NELZE OBNOVIT!**
:end

:info_warning_temp_help:
*Vysoká teplota disku* určuje práh pro varování při vysoké teplotě tohoto disku.  
Překročení této hodnoty spustí varovné oznámení.

Hodnota 0 deaktivuje kontrolu vysoké teploty disku (včetně oznámení).
:end

:info_critical_temp_help:
*Kritická teplota disku* určuje práh pro kriticky vysokou teplotu tohoto disku.  
Překročení této hodnoty spustí výstražné (alarmové) oznámení.

Hodnota 0 deaktivuje kontrolu kritické teploty disku (včetně oznámení).
:end

:info_file_system_help:
Zvol požadovaný souborový systém. Změna souborového systému ti umožní tento disk naformátovat novým vybraným systémem.  
Upozorňujeme, že **všechna stávající data na disku budou smazána**.
:end

:info_comments_help:
Tento text se zobrazí v poli *Komentář* ve Windows Průzkumníku.  
Zadej libovolný text až do délky 256 znaků.
:end

:info_warning_utilization_help:
*Vysoké využití disku* určuje práh pro varování při vysokém využití tohoto disku.  
Překročení této hodnoty spustí varovné oznámení.

Pokud je práh pro vysoké využití nastaven na stejnou nebo vyšší hodnotu než práh pro kritické využití,  
bude se zobrazovat pouze kritické oznámení (žádné varování pro vysoké využití).

Hodnota 0 deaktivuje práh pro kritické využití disku (včetně oznámení).
:end

:info_critical_utilization_help:
*Kritické využití disku* určuje práh pro kriticky vysoké využití tohoto disku.  
Překročení této hodnoty spustí výstražné (alarmové) oznámení.

Hodnota 0 deaktivuje práh pro kritické využití disku (včetně oznámení).
:end


:info_btrfs_balance_help:
**Vyvážení (Balance)** spouští program *btrfs balance*, který přerozdělí data mezi všemi disky v poolu.  
Například slouží ke konverzi poolu z režimu raid1 na raid0 nebo naopak.

Při provedení *úplného vyvážení* (full balance) se v podstatě všechna data v souborovém systému přepíšou.

*Konverze režimu* ovlivňuje datové oblasti btrfs; metadata vždy používají raid1 a v případě potřeby jsou během procesu vyvážení převedena na raid1.

Tento proces může trvat delší dobu v závislosti na velikosti souborového systému a rychlosti disku.

Unraid OS používá při vytváření poolu s více disky následující výchozí volby:

`-dconvert=raid1 -mconvert=raid1`

Podrobnější dokumentaci k příkazu "btrfs balance" najdeš v [manuálu btrfs](https://btrfs.wiki.kernel.org/index.php/Manpage/btrfs-balance)

*Poznámka: režimy raid5 a raid6 jsou komunitou Linuxu stále považovány za **experimentální***
:end


:info_balance_cancel_help:
**Zrušit** přeruší právě probíhající proces vyvážení (balance).
:end


:info_btrfs_scrub_help:
**Scrub** spustí program *btrfs scrub*, který čte všechny datové a metadatové bloky ze všech disků  
a ověřuje jejich kontrolní součty (checksums).

Pokud je vybrána možnost *Opravit poškozené bloky*, *btrfs scrub* se pokusí opravit všechny chybné bloky,  
pokud je k dispozici bezchybná kopie.
:end


:info_scrub_cancel_help:
**Zrušit** přeruší právě probíhající proces kontroly (scrub).
:end

:info_btrfs_check_help:
**Kontrola** spustí nástroj *btrfs check* pro ověření integrity souborového systému na disku.

Pole *Možnosti* je předvyplněno volbou *--readonly*, která provede pouze kontrolu bez úprav.  
Pokud je nutná oprava, spusť nástroj znovu s volbou *--repair* – ta umožní nástroji *btrfs check* souborový systém opravit.

Po spuštění kontroly je vhodné stránku znovu načíst, abys mohl(a) sledovat průběh a stav procesu.  
V závislosti na velikosti souborového systému a typu chyb na disku může tento proces **trvat velmi dlouho** (i několik hodin).

Výstup je minimální, ale průběh lze sledovat podle stoupajícího počtu operací čtení/zápisu na hlavní stránce.  
Ve výstupním okně se zobrazí jen omezené informace, ale můžeš ověřit běh procesu pomocí čítačů čtení/zápisu pro daný disk.
:end


:info_check_cancel_help:
**Zrušit** přeruší právě probíhající kontrolu (check).
:end


:info_reiserfs_check_help:
**Kontrola** spustí nástroj *reiserfsck* pro ověření integrity souborového systému na disku.

Pole *Možnosti* lze vyplnit specifickými parametry pro opravu problémů v souborovém systému.  
Obvykle se první kontrola provádí s prázdným polem *Možnosti*. Pokud první kontrola zjistí problémy,  
musíš spustit druhou kontrolu s jednou z voleb, které byly doporučeny při prvním běhu *reiserfsck*.

Po spuštění kontroly prosím aktualizuj stránku pro sledování stavu.  
V závislosti na velikosti souborového systému a chybách na disku může tento proces **trvat velmi dlouho** (i několik hodin).  
Výstup je minimální, ale průběh můžeš sledovat pomocí čítačů čtení/zápisu na hlavní stránce.
:end


:info_reiserfs_cancel_help:
**Zrušit** přeruší právě probíhající kontrolu.
:end


:info_xfs_check_help:
**Kontrola** spustí nástroj *xfs_repair* pro ověření integrity souborového systému na disku.

Pole *Možnosti* je předvyplněno volbou *-n*, která provede pouze kontrolu bez úprav.  
Pokud je nutná oprava, spusť nástroj znovu s prázdným polem *Možnosti* – tím povolíš nástroji *xfs_repair* opravit souborový systém.

Po spuštění kontroly prosím aktualizuj stránku, abys mohl(a) sledovat stav.  
V závislosti na velikosti souborového systému a chybách na disku může tento proces **trvat velmi dlouho** (i několik hodin).  
Výstup je minimální, ale průběh můžeš sledovat podle čítačů čtení/zápisu na hlavní stránce.
:end


:info_xfs_cancel_help:
**Zrušit** přeruší právě probíhající kontrolu (xfs_repair).
:end

:info_smart_notifications_help:
SMART oznámení jsou generována buď při zvyšující se RAW hodnotě atributu, nebo při snižující se NORMALIZOVANÉ prahové hodnotě,  
která je definována výrobcem.

Každý disk může mít svou vlastní hodnotu, která přepíše "výchozí" hodnotu (viz globální nastavení SMART v nastavení disků).
:end


:info_tolerance_level_help:
Lze nastavit prahovou toleranci, aby malé změny nevyvolávaly oznámení.  
Pokud je tato hodnota nastavena příliš vysoko, mohou kritické změny proběhnout bez upozornění.

Každý disk může mít svou vlastní hodnotu, která přepíše "výchozí" hodnotu (viz globální nastavení SMART v nastavení disků).
:end


:info_controller_type_help:
Ve výchozím nastavení provádí výběr řadiče nástroj smartctl, aby bylo možné načíst SMART informace.  
Některé řadiče však vyžadují speciální nastavení, aby smartctl fungoval správně.  
Pomocí tohoto nastavení můžeš zvolit typ řadiče a přizpůsobit specifický index disku a název dle své konfigurace.  
Podívej se do dokumentace výrobce, kde najdeš potřebné informace.

Každý disk může mít svou vlastní hodnotu, která přepíše "výchozí" nastavení (viz globální SMART nastavení v sekci nastavení disků).
:end


:info_attribute_notifications_help:
Zde můžeš povolit nebo zakázat oznámení pro zvolená SMART atributa.  
Doporučuje se ponechat výchozí nastavení, tedy VŠECHNA vybraná atributa.

Pokud některé atributy na tvém disku nejsou dostupné nebo neposkytují správné informace, můžeš je vyloučit.  
Zároveň můžeš zadat vlastní SMART atributy, které budou generovat oznámení – buď však při výběru opatrný/á.  
Nevhodně zvolená SMART atributa mohou vést k zahlcení systémem oznámení.

Každý disk může mít svou vlastní hodnotu, která přepíše "výchozí" nastavení (viz globální SMART nastavení v sekci nastavení disků).
:end


:selftest_history_help:
Klikni na **Zobrazit**, abys zobrazil(a) historii SMART samodiagnostiky uloženou přímo na disku.  
Tato funkce je dostupná pouze tehdy, pokud je disk aktivní.
:end

:selftest_error_log_help:
Klikni na **Zobrazit**, abys zobrazil(a) chybový záznam uložený přímo na disku.  
Tato funkce je dostupná pouze tehdy, pokud je disk aktivní.
:end


:selftest_short_test_help:
Spustí *krátký* SMART samodiagnostický test. Předpokládaná doba trvání je uvedena v sekci *Funkce*.  
Obvykle trvá několik minut.

Pokud bude disk uspán, test se přeruší.  
Tato funkce je dostupná pouze tehdy, pokud je disk aktivní.
:end


:selftest_long_test_help:
Spustí *rozšířený* SMART samodiagnostický test. Předpokládaná doba trvání je uvedena v sekci *Funkce*.  
Obvykle trvá několik hodin.

Pokud bude disk uspán, test se přeruší.  
Doporučuje se nastavit zpoždění uspání disku na „Nikdy“, aby nedošlo k přerušení testu.

Tato funkce je dostupná pouze tehdy, pokud je disk aktivní.
:end


:selftest_result_help:
Pokud neběží žádný test, zobrazí se zde poslední výsledek SMART samodiagnostiky (pokud je dostupný).  
V opačném případě se zobrazí ukazatel průběhu (v procentech) pro právě probíhající test.
:end


:smart_attributes_help:
Tento seznam zobrazuje SMART atributy, které daný disk podporuje.  
Pro podrobnější informace o jednotlivých atributech se doporučuje vyhledat je na internetu.

Atributy označené *oranžově* vyžadují tvou pozornost. Mají **RAW hodnotu** vyšší než nula a mohou znamenat blížící se selhání disku.

Zvláštní pozornost věnuj případům, kdy se hodnota v čase zvyšuje. V případě pochybností se obrať na fórum Limetech.
:end


:smart_capabilities_help:
Tento seznam zobrazuje SMART funkce, které daný disk podporuje.

Zde je uvedena také odhadovaná délka krátké a rozšířené SMART samodiagnostiky.
:end


:smart_identity_help:
Tento seznam zobrazuje identifikační SMART informace o tomto disku.
:end

:open_devices_help:
Jedná se o disky, které jsou nainstalovány ve tvém serveru, ale nejsou přiřazeny ani do paritně chráněného pole,  
ani do cache poolu.
:end


:flash_backup_help:
Použij *Zálohu flash disku* k vytvoření jednoho zip souboru s kompletním aktuálním obsahem flash disku  
a jeho uložení do počítače.
:end


:syslinux_cfg_help:
Na této stránce můžeš upravit soubor „syslinux.cfg“.  
Projevení změn vyžaduje restart serveru.
:end


:syslinux_button_help:
Klikni na tlačítko **Výchozí**, aby se do editoru načetl továrně přednastavený obsah.  
Pro použití změn musíš ještě kliknout na **Použít**.

Klikni na tlačítko **Použít**, abys uložil(a) aktuální změny.  
Klikni na **Obnovit**, pokud chceš vrátit změny před uložením.  
Klikni na **Hotovo**, pro opuštění této stránky.
:end


:info_share_assignment_help:
Vybraný pool je dostupný pro uživatelské sdílené složky.
:end


:info_free_space_help:
Tímto se nastaví „minimální hranice“ pro zbývající volné místo na svazku.  
Pokud volné místo klesne pod tuto hodnotu, zobrazí se při pokusu o zápis nových souborů přes uživatelské sdílení chybová hláška „nedostatek volného místa“.

Zadej číselnou hodnotu s jednou z následujících jednotek:

**KB** = 1.000<br>
**MB** = 1.000.000<br>
**GB** = 1.000.000.000<br>
**TB** = 1.000.000.000.000<br>
**K** = 1.024 (d.h., 2^10)<br>
**M** = 1.048.576 (d.h., 2^20)<br>
**G** = 1.073.741.824 (d.h., 2^30)<br>
**T** = 1.099.511.627.776 (d.h., 2^40)<br>

Pokud jednotka není uvedena, použije se hodnota v blocích o velikosti 1024 bajtů.
:end


:share_list_help:
**Barevný stavový indikátor** – Význam barevného indikátoru na začátku každého řádku *uživatelských sdílení* je následující:

<i class='fa fa-circle orb green-orb'></i>Všechny soubory se nacházejí na chráněném úložišti s ochranou proti selhání.

<i class='fa fa-warning orb yellow-orb'></i>Některé nebo všechny soubory se nacházejí na nechráněném úložišti.

**Úrovně zabezpečení:**

+ "-" – Sdílení není exportováno a není přístupné ze sítě  
+ *Veřejné* – Všichni uživatelé včetně hostů mají plný přístup pro čtení/zápis (veřejný přístup)  
+ *Chráněné* – Všichni uživatelé včetně hostů mají přístup pro čtení. Zápis je povolen individuálně (omezený přístup)  
+ *Soukromé* – Žádný přístup pro hosty. Přístup pro čtení/zápis nebo pouze pro čtení je nastaven individuálně (uzavřený přístup)

**Speciální úrovně:**

+ Úroveň zabezpečení SMB zobrazená *kurzívou* označuje exportované skryté sdílení.  
+ NFS nemá žádné speciální úrovně zabezpečení pro uživatelská sdílení.
:end


:disk_list_help:
**Barevný stavový indikátor** – Význam barevného indikátoru na začátku každého řádku *sdílení disků* je následující:

<i class='fa fa-circle orb green-orb'></i>Připojený disk je chráněn (součástí pole s redundancí).

<i class='fa fa-warning orb yellow-orb'></i>Připojený disk není chráněn (mimo paritní pole).

**Úrovně zabezpečení:**

+ "-" – Sdílení není exportováno a není přístupné ze sítě  
+ *Veřejné* – Všichni uživatelé včetně hostů mají plný přístup pro čtení/zápis (veřejný přístup)  
+ *Chráněné* – Všichni uživatelé včetně hostů mají přístup pro čtení. Zápis je povolen individuálně (omezený přístup)  
+ *Soukromé* – Žádný přístup pro hosty. Přístup pro čtení/zápis nebo pouze pro čtení je nastaven individuálně (uzavřený přístup)

**Speciální úrovně:**

+ Úroveň zabezpečení SMB zobrazená *kurzívou* označuje exportované skryté sdílení.  
+ NFS nemá žádné speciální úrovně zabezpečení pro disková sdílení.
:end


:share_edit_global1_help:
*Sdílení* (nebo také *uživatelské sdílení*) je název složky v kořenovém adresáři na úložném svazku (disk pole nebo pool).  
Každé sdílení může být exportováno pro síťový přístup. Při procházení sdílení se zobrazí sloučený pohled na všechny soubory a podadresáře,  
které existují v dané složce na každém úložném disku.

Funkce *Načíst nastavení z* slouží k převzetí nastavení existujícího sdílení pro nové sdílení.

Zadej požadovaný název sdílení a klikni na **Načíst**, čímž zkopíruješ nastavení ze zvoleného zdroje.
:end


:share_edit_global2_help:
*Zkopírovat nastavení do* slouží k přenosu nastavení aktuálního sdílení na jedno nebo více existujících sdílení.

Vyber požadované cíle a klikni na **Kopírovat**, aby se nastavení aplikovalo na vybraná sdílení.
:end


:share_edit_name_help:
Název sdílení může mít maximálně 40 znaků, rozlišuje malá a velká písmena a podléhá následujícím omezením:

* nesmí obsahovat uvozovky (") ani tyto znaky: / \ * < > |  
* nesmí být jedním z rezervovaných názvů sdílení: flash, cache, cache2, .., disk1, disk2, ...

Důrazně doporučujeme zvolit jednoduchý název a vyhnout se speciálním znakům.
:end


:share_edit_comments_help:
Libovolný text, maximálně 256 znaků.
:end


:share_edit_allocation_method_help:
Toto nastavení určuje, jak Unraid OS vybírá disk, který se použije při vytváření nových souborů nebo složek:

**High-Water**  
Použije první disk, který má stále více volného místa než aktuální *High-Water hranice*.  
*High-Water hranice* je počítána jako polovina velikosti největšího datového disku.  
Pokud žádný disk nemá dostatek prostoru nad touto hranicí, hranice se znovu dělí dvěma a proces se opakuje.

Cílem metody **High-Water** je zapisovat co nejvíce dat na jednotlivé disky (aby se omezilo časté zapínání disků)  
a zároveň udržovat jejich zaplnění přibližně na stejné úrovni  
(aby bylo pole rovnoměrně zaplněné).

**Fill-Up**  
Použije disk, který má stále volné místo nad hodnotou **Minimální volné místo**.

**Most-Free**  
Použije disk s aktuálně největším volným prostorem.
:end


:share_edit_free_space_help:
*Minimální volné místo*, které musí být k dispozici pro zápis na jakýkoli disk přiřazený ke sdílení.<br>

Zadej hodnotu, která je stejná nebo větší než největší jednotlivý soubor, který chceš do sdílení zkopírovat.  
Případně uveď jednotky jako KB, MB, GB nebo TB, např. 10MB.
:end


:share_edit_split_level_help:
Určuje, zda může být složka rozdělena mezi více disků.

**Každou složku automaticky rozdělit, pokud je to nutné**  
Když se ve sdílení vytvoří nový soubor nebo podsložka, Unraid OS nejprve vybere disk podle nastavené *metody rozdělení*.  
Pokud nadřazená složka (která má obsahovat nový soubor nebo podsložku) na vybraném disku ještě neexistuje,  
Unraid nejprve vytvoří všechny potřebné nadřazené složky a poté i nový soubor nebo podsložku.

**Hlavní složku automaticky rozdělit, pokud je to nutné**  
Pokud se ve sdílení vytvoří nový soubor nebo podsložka přímo v hlavní složce,  
a ta ještě neexistuje na daném disku, vytvoří se nejprve tato podsložka.  
Při vytváření dalších úrovní pod touto složkou bude nový obsah uložen na stejný disk, jako hlavní složka.

**Rozdělit složku po „N“ úrovních, pokud je to nutné**  
Podobně jako výše: pokud se nový soubor nebo podsložka nachází ve složce na úrovni "N"  
a tato složka na daném disku zatím není, Unraid vytvoří všechny nadřazené složky.  
Pokud je ale nová složka nebo soubor umístěn pod úrovní "N", bude uložen na stejný disk, kde je jeho nadřazená složka.

**Ruční: Složky nerozdělovat automaticky**  
Pokud je potřeba vytvořit novou složku nebo soubor, Unraid ji vytvoří pouze na tom disku,  
kde se již nachází odpovídající hlavní složka.
:end


:share_edit_included_disks_help:
Zadej disky, které mohou být použity pro toto sdílení.  
Ve výchozím nastavení jsou zahrnuty všechny disky — pokud žádné konkrétní nevybereš, může být sdílení rozšířeno na *všechny* disky pole.
:end


:share_edit_excluded_disks_help:
Zadej disky, které *nesmí* být pro toto sdílení použity.  
Ve výchozím nastavení nejsou vyloučeny žádné disky.
:end


:share_edit_cache_pool_help:
Určuje, zda mohou být nové soubory a složky zapisovány na cache disk/pool (pokud je k dispozici).  
Toto nastavení ovlivňuje chování nástroje *Mover*.

**Ne** zakáže zápis nových souborů a složek na cache disk/pool.  
*Nepřesouvá se nic pomocí Moveru* – všechny soubory zůstávají, kde byly.

**Ano** umožní zápis nových souborů a složek na cache disk/pool, pokud je na něm dostatek volného místa.  
Pokud není dostatek místa, budou soubory a složky zapsány přímo na pole (Array).  
Pokud je aktivní *Mover*, přemístí soubory z cache na pole.

**Pouze** znamená, že nové soubory a složky *musí být* zapsány na cache disk/pool.  
Pokud na cache není dostatek místa, zápis selže s chybou *není dostatek volného místa*.  
*Mover není aktivní* – ignoruje všechny soubory, které by se nacházely na poli.

**Preferovat** znamená, že nové soubory a složky se mají zapisovat na cache disk/pool,  
pokud je na něm dostatek volného místa.  
Pokud ne, zapisuje se přímo na pole.  
Pokud je aktivní *Mover*, přemístí soubory z pole zpět na cache.

**POZNÁMKA:** Mover může přesouvat pouze ty soubory, které nejsou právě otevřené nebo používané.  
To znamená, že pokud chceš přesunout soubory systémových služeb jako Docker nebo virtuální stroje (VMs),  
musíš tyto služby během běhu Moveru zastavit.
:end

:share_edit_copy_on_write_help:
Nastav na **Ne**, pokud chceš, aby *btrfs* nastavil atribut NOCOW (No Copy-on-Write) pro tuto sdílenou složku,  
pokud je vytvořena na souborovém systému formátovaném jako *btrfs*.  
Pokud je tato možnost aktivní, nově vytvořené soubory a složky zdědí atribut NOCOW.  
Toto nastavení doporučujeme pro sdílení, která slouží k ukládání image souborů virtuálních strojů (VM)  
a souborů typu loopback pro Docker.  
Toto nastavení nemá žádný vliv na souborové systémy jiné než btrfs.

Nastav na **Auto** pro standardní chování – COW bude **aktivní** pro všechny disky formátované jako *btrfs*.
:end

:share_edit_status_help:
Sdílení neobsahuje *žádná* data a může být odstraněno, pokud již není potřeba.
:end

:share_edit_delete_help:
Sdílení *nelze* odstranit, dokud na něm zůstávají data.  
Pozor – počítají se i skryté soubory, viz [SMB nastavení](/Settings/SMB) → Skrýt „dot“ soubory.
:end


:smb_security_help:
*Načíst nastavení z* slouží k převzetí nastavení SMB zabezpečení z jiné existující sdílené složky.

Vyber požadovaný název sdílení a klikni na **Načíst**, čímž zkopíruješ nastavení SMB zabezpečení ze zvoleného sdílení.

*Zapsat nastavení do* slouží k aplikaci aktuálního nastavení SMB zabezpečení na jedno nebo více jiných existujících sdílení.

Vyber požadovaná sdílení a klikni na **Zapsat**, čímž zkopíruješ aktuální SMB zabezpečení do vybraných složek.
:end


:smb_export_help:
Určuje, zda je sdílení viditelné a/nebo přístupné. Nastavení „Ano (skryté)“ *skryje* sdílení při *procházení*,  
ale bude nadále dostupné, pokud znáš jeho název.
:end


:smb_time_machine_volume_help:
Omezuje zobrazovaný volný prostor a zabrání například aplikaci Time Machine využít celý volný prostor pro zálohy.  
Pokud nastavíš hodnotu na „1024“, bude hlášený dostupný prostor 1 GB.
:end


:smb_case_sensitive_names_help:
Určuje, zda se při práci s názvy souborů rozlišují velká a malá písmena.

Výchozí nastavení **Auto** umožňuje klientům, kteří podporují rozlišení velikosti písmen (např. Linux CIFSVFS),  
informovat Samba server, že chtějí přistupovat k souborovému systému s rozlišením velikosti písmen (UNIX-style).  
Žádný Windows systém nepodporuje názvy souborů s rozlišením velikosti písmen, takže nastavení **Auto** se chová stejně jako „Ne“ –  
názvy ze systému Windows budou serveru předány správně.  
Toto nastavení může zpomalit práci se složkami s mnoha soubory, protože Samba provádí hledání názvů.

Nastavení **Ano** znamená, že názvy souborů zůstanou přesně tak, jak je klient předá –  
a budou uloženy i načítány pouze v této podobě.  
Tím se zrychlí přístup k velkým adresářům, ale některé aplikace pro Windows s tímto nastavením nemusí fungovat správně.  
Například pokud vytvoříš soubor „MojeSoubor“ a aplikace z Windows se pokusí otevřít „MOJESOUBOR“, soubor nebude nalezen.

Nastavení **Pouze malá písmena** znamená, že všechny názvy souborů – nejen nových – budou převedeny na malá písmena.  
Jinými slovy, jakýkoli název vytvořený z Windows bude uložen a čten pouze jako malá písmena.  
Tím je zajištěno, že aplikace ve Windows vždy správně naleznou všechny soubory, bez ohledu na velikost písmen.  
Toto nastavení ale neovlivňuje seznam složek.  
Upozornění: toto nastavení by mělo být použito pouze při vytváření nové sdílené složky.
:end


:smb_security_modes_help:
Přehled nastavení zabezpečení:

**Veřejné** – Všichni uživatelé včetně hostů mají práva pro čtení i zápis.

**Chráněné** – Všichni uživatelé včetně hostů mají práva pro čtení,  
práva pro zápis nastavuješ individuálně pro jednotlivé uživatele.

**Soukromé** – Hosté nemají přístup. Práva pro čtení a/nebo zápis nastavuješ individuálně pro jednotlivé uživatele.
:end


:smb_secure_access_help:
*Načíst nastavení z* slouží k převzetí nastavení SMB přístupu uživatelů z existující sdílené složky do aktuálně vybrané sdílené složky.

Vyber požadované sdílení a klikni na **Načíst**, čímž zkopíruješ nastavení uživatelského přístupu ze zvoleného zdroje.

*Zapsat nastavení do* slouží k přenosu aktuálního nastavení SMB přístupu uživatelů do jedné nebo více dalších existujících sdílení.

Vyber požadované cíle a klikni na **Zapsat**, čímž aplikuješ nastavení přístupu na vybraná sdílení.
:end


:smb_private_access_help:
*Načíst nastavení z* slouží k převzetí nastavení SMB přístupových práv aktuálního sdílení ze zvolené existující sdílené složky.

Vyber požadovaný název sdílení a klikni na **Načíst**, čímž zkopíruješ nastavení zabezpečení SMB ze zvoleného zdroje.

*Zapsat nastavení do* slouží k přenosu aktuálního nastavení přístupových práv SMB do jedné nebo více jiných existujících sdílení.

Vyber cílová sdílení a klikni na **Zapsat**, čímž aplikuješ přístupová práva na vybraná sdílení.
:end


:nfs_security_help:
*Načíst nastavení z* slouží k převzetí nastavení NFS zabezpečení aktuálně vybrané sdílené složky z jiné existující sdílené složky.

Vyber požadovaný název sdílení a klikni na **Načíst**, čímž zkopíruješ nastavení NFS zabezpečení ze zvoleného zdroje.

*Zapsat nastavení do* slouží k přenosu aktuálního nastavení NFS zabezpečení do jedné nebo více dalších existujících sdílení.

Vyber cílová sdílení a klikni na **Zapsat**, čímž aplikuješ nastavení NFS zabezpečení na vybraná sdílení.
:end


:user_add_username_help:
Uživatelské jméno může mít maximálně 40 znaků a musí začínat **malým písmenem** nebo podtržítkem,  
následovat mohou **malá písmena**, číslice, podtržítka nebo pomlčky.  
Uživatelské jméno může končit znakem dolaru ($).
:end


:user_add_description_help:
Maximálně 64 znaků. Znaky ampersand (&amp;), uvozovky (&quot;) a dvojtečka (:) nejsou povoleny.
:end

:user_add_custom_image_help:
Obrázek bude přizpůsoben na velikost 48×48 pixelů. Maximální velikost nahrávaného obrázku je 95 kB (97 280 bajtů).
:end


:user_password_help:
Maximálně 128 znaků.
:end


:user_edit_description_help:
Maximálně 64 znaků. Znaky ampersand (&amp;), uvozovky (&quot;) a dvojtečka (:) nejsou povoleny.
:end


:user_edit_custom_image_help:
Obrázek bude přizpůsoben na velikost 48×48 pixelů. Maximální velikost nahrávaného obrázku je 512 kB (524,288 bajtů).
:end


:cpu_vms_help:
Tato stránka zobrazuje celkový přehled aktuálního přiřazení CPU pro virtuální stroje (VMs).<br>  
Umožňuje také provádět změny v přiřazení.

Běžící VM budou **nejprve zastaveny** a po změně znovu spuštěny.<br>  
Pozastavené VM budou upraveny okamžitě a nové přiřazení se projeví při dalším spuštění.

Po kliknutí na ***Použít*** proběhne kontrola změn. Pouze VM, u kterých došlo ke změnám, budou upraveny paralelně.

*Důležité: Před opuštěním stránky vyčkej, dokud nebudou všechny změny dokončeny.*
:end


:cpu_pinning_help:
Tato stránka zobrazuje celkový přehled aktuálního přiřazení CPU pro Docker kontejnery.<br>  
Umožňuje také provádět změny těchto přiřazení.

Běžící kontejnery budou **nejprve zastaveny** a po změně znovu spuštěny.<br>  
Pozastavené kontejnery budou upraveny okamžitě a nové přiřazení se projeví při jejich ručním spuštění.

Po kliknutí na ***Použít*** proběhne kontrola změn. Poté budou všechny kontejnery, kterých se změna týká, upraveny paralelně.

*Důležité: Před opuštěním stránky vyčkej, dokud nebudou všechny změny dokončeny.*

Ve výchozím nastavení nejsou pro Docker kontejner vybrány **žádné** CPU jádra.  
To znamená, že má přístup ke **všem dostupným** jádrům.<br>  
Nech **žádná jádra nevybraná**, pokud chceš, aby kontejner mohl využívat všechna jádra bez omezení.

Nevybírej jádra pro kontejnery, které mají být *izolovány*.  
Pokud to nedodržíš, může kontejner při výběru více izolovaných jader použít pouze jedno z nich (to s nejnižším číslem).<br>  
Obvykle to není žádoucí – pokud vybereš více jader, měly by být vhodně nakonfigurovány.
:end


:cpu_isolation_help:
Izolace CPU umožňuje určit jádra, která mají být výhradně rezervována pro přiřazení (např. pro VMs nebo Docker kontejnery).

To je mimořádně důležité pro plynulý chod herních virtuálních strojů.  
I když ručně přiřadíš Docker kontejnery tak, aby se nepřekrývaly s jádry používanými herním VM,  
hostitelský operační systém může stále tato jádra využívat pro obsluhu webového rozhraní, provádění paritní kontroly,  
btrfs operace apod. – čímž může výkon VM negativně ovlivnit.
:end


:timezone_help:
Vyber své časové pásmo z rozbalovacího seznamu.
:end


:use_ntp_help:
Zvol **Ano**, pokud chceš používat síťový časový protokol (NTP), aby serverový čas zůstal přesný.  
Důrazně doporučujeme použití síťového časového serveru, zejména pokud plánuješ používat Active Directory.

Poznámka: Pokud používáš časové servery `pool.ntp.org`, přečti si také jejich [dokumentaci](http://www.pool.ntp.org/en/use.html).
:end


:ntp_server1_help:
Primární NTP server. Zadej plně kvalifikovaný název domény (FQDN) nebo IP adresu.
:end


:ntp_server2_help:
Alternativní NTP server, který se použije v případě výpadku NTP serveru 1.
:end

:ntp_server3_help:
Alternativní NTP server, který se použije, pokud selžou NTP servery 1 a 2.
:end

:ntp_server4_help:
Alternativní NTP server, který se použije, pokud selžou NTP servery 1, 2 a 3.
:end

:current_time_help:
Zadej aktuální čas. Použij formát RRRR-MM-DD HH:MM:SS.  
Pokud je aktivní NTP, pole bude nedostupné.
:end

:disk_enable_autostart_help:
Pokud zvolíš **Ano**, pole se automaticky spustí a sdílení se exportují,  
pokud je konfigurace zařízení při startu serveru správná.<br>  
Pokud zvolíš **Ne**, je nutné pole spustit ručně.
:end

:disk_spindown_delay_help:
Určuje výchozí časový limit pro uspání disku po období bez aktivity (žádné I/O operace).  
Tento výchozí čas lze přepsat pro každý jednotlivý disk v jeho vlastním nastavení.
:end

:disk_spinup_groups_help:
Pokud zvolíš **Ano**, bude aktivní funkce [Spinup Groups](/Help).
:end

:disk_default_partition_format_help:
Určuje typ rozvržení oddílů, které bude použito při formátování disků s kapacitou **do 2 TB včetně**.  
(Všechna zařízení větší než 2 TB budou vždy formátována s GPT tabulkou oddílů.)

**MBR: nevyrovnané** – Vytvoří oddíl ve stylu MBR, přičemž oddíl 1 začíná na **63. sektoru**.  
Toto je *tradiční* nastavení používané prakticky ve všech starších MBR konfiguracích.

**MBR: zarovnané na 4K** – Vytvoří oddíl ve stylu MBR, kde oddíl 1 začíná na **64. sektoru**.  
Vzhledem k tomu, že sektor má 512 bajtů, je začátek oddílu zarovnán na hranici 4 KB.  
Toto je požadováno pro správnou podporu tzv. *Advanced Format* disků.

Neměň toto nastavení, pokud nemáš specifické důvody mimo běžný standard **MBR: zarovnané na 4K**.
:end

:disk_default_file_system_help:
Určuje výchozí formát souborového systému, který bude použit při formátování *nepřipojitelného* disku v poli.

Výchozí souborový systém pro jednozařízeníový nebo víczařízeníový cache je vždy Btrfs.
:end

:disk_shutdown_timeout_help:
Určuje, jak dlouho se čeká na *čisté* vypnutí systému před tím, než se vynutí vypnutí,  
pokud bylo spuštěno ruční vypnutí serveru.
:end

:disk_tunable_poll_attributes_help:
Určuje interval dotazování na SMART atributy disku (v sekundách).  
Hodnota 0 dotazování SMART deaktivuje (nedoporučuje se).
:end

:disk_tunable_nr_requests_help:
Určuje hodnotu `nr_requests` ovladače zařízení pro všechny disky v poli.

**Auto** ponechá aktuální nastavení každého disku tak, jak je.

Poznámka: Pokud toto pole zůstane prázdné a klikneš na **Použít**, nastaví se výchozí hodnota.  
Po výběru Auto je nutné provést restart, aby se nastavení projevilo.
:end

:disk_tunable_nr_requests_help:
Určuje hodnotu `nr_requests` ovladače zařízení pro všechny disky v poli.

**Auto** ponechá aktuální nastavení každého disku tak, jak je.

Poznámka: Pokud toto pole zůstane prázdné a klikneš na **Použít**, nastaví se výchozí hodnota.  
Po výběru Auto je nutné provést restart, aby se nastavení projevilo.
:end

:disk_tunable_scheduler_help:
Určuje, jaký plánovač I/O operací jádra se použije pro všechny disky v poli.

**Auto** ponechá aktuální plánovač každého zařízení (výchozí: mq-deadline).

Poznámka: Po výběru Auto je nutné provést restart, aby se nastavení projevilo.
:end

:disk_tunable_md_num_stripes_help:
Určuje velikost *stripe poolu* v počtu *stripe* bloků. Stripe je datová struktura,  
která umožňuje paralelní 4K čtení/zápis a je nezbytná pro pole chráněné paritou.

Poznámka: Pokud toto pole zůstane prázdné a klikneš na **Použít**, nastaví se výchozí hodnota.  
Změna se projeví po restartu.
:end

:disk_tunable_md_queue_limit_help:
Číslo v rozsahu [1–100], které určuje maximální procentuální zatížení stripe poolu.

Poznámka: Pokud toto pole zůstane prázdné a klikneš na **Použít**, nastaví se výchozí hodnota.  
Změna se projeví po restartu.
:end

:disk_tunable_md_sync_limit_help:
Číslo v rozsahu [0–100], které určuje maximální procentuální zatížení stripe poolu  
vyhrazeného pro synchronizaci/ověření parity, pokud současně probíhají jiné I/O operace.

Poznámka: Pokud toto pole zůstane prázdné a klikneš na **Použít**, nastaví se výchozí hodnota.  
Změna se projeví po restartu.
:end

:disk_tunable_md_write_method_help:
Určuje metodu zápisu používanou při psaní na aktivní disk v poli chráněném paritou.

*Auto* automaticky zvolí metodu `read/modify/write`.
:end

:disk_default_warning_utilization_help:
*Upozornění na vytížení disku* nastavuje výchozí varovný práh pro zaplnění všech disků.  
Je-li tento práh překročen, zobrazí se varovná zpráva.

Pokud je varovný práh roven nebo vyšší než kritický, budou zobrazována pouze kritická upozornění.

Hodnota 0 varovný práh (a tedy i upozornění) deaktivuje.
:end

:disk_default_critical_utilization_help:
*Kritické vytížení disku* nastavuje kritický výchozí práh zaplnění pro všechny disky.  
Je-li překročen, zobrazí se kritické varování.

Hodnota 0 tento kritický práh (a oznámení) deaktivuje.
:end

:disk_default_warning_temperature_help:
*Varování na teplotu disku* nastavuje výchozí varovný práh pro teplotu všech disků.  
Je-li překročen, zobrazí se varovná zpráva.

Hodnota 0 varovný práh (včetně upozornění) deaktivuje.
:end

:disk_default_critical_temperature_help:
*Kritická teplota disku* nastavuje výchozí kritický práh pro teplotu všech disků.  
Překročení této hodnoty spustí kritické varování.

Hodnota 0 tento kritický práh (a oznámení) deaktivuje.
:end

:disk_default_smart_notification_help:
SMART oznámení jsou generována buď při zvyšující se hodnotě RAW atributu,  
nebo při klesající NORMALIZOVANÉ hodnotě, která dosáhne výrobcem definovaného prahu.

Tato sekce určuje globální nastavení pro všechny disky.  
Je možné nastavit hodnoty i individuálně pro jednotlivé disky.
:end

:disk_default_smart_tolerance_help:
Lze nastavit úroveň tolerance, která zabrání tomu, aby malé změny vyvolávaly oznámení.  
Příliš vysoká tolerance ale může způsobit, že kritické změny zůstanou bez upozornění.

Tato sekce určuje globální nastavení pro všechny disky.  
Je možné nastavit hodnoty i individuálně pro jednotlivé disky.
:end

:disk_default_smart_controller_help:
Ve výchozím nastavení smartctl automaticky vybírá řadič pro čtení SMART informací.  
Některé řadiče však vyžadují specifická nastavení, aby smartctl fungoval správně.  
Pomocí tohoto pole můžeš určit typ řadiče, index disku a název zařízení dle potřeby.  
Potřebné údaje nalezneš v dokumentaci výrobce.

Tato sekce určuje globální nastavení pro všechny disky.  
Je možné nastavit hodnoty i individuálně pro jednotlivé disky.
:end

:disk_default_smart_attribute_help:
Uživatel může povolit nebo zakázat oznámení pro vybrané SMART atributy.  
Doporučujeme ponechat výchozí nastavení – tedy oznámení pro VŠECHNY vybrané atributy.

Pokud některé atributy na disku neexistují nebo poskytují zavádějící informace, mohou být vyloučeny.  
Lze také definovat vlastní SMART atributy pro generování oznámení.  
Buď ale opatrný – nesprávný výběr může způsobit zahlcení oznámeními.

Tato sekce určuje globální nastavení pro všechny disky.  
Je možné nastavit hodnoty i individuálně pro jednotlivé disky.
:end

:docker_repositories_help:
Použij toto pole pro přidání vlastních repozitářů šablon.  
Docker šablony slouží k usnadnění vytváření a opětovného nasazení Docker kontejnerů. Jedna šablona na řádek.

Seznam populárních komunitou podporovaných repozitářů nalezneš zde:  
<a href="http://lime-technology.com/forum/index.php?topic=37958.0" target="_blank">http://lime-technology.com/forum/index.php?topic=37958.0</a>
:end


:docker_enable_help:
Před prvním spuštěním Docker služby musíš zadat umístění image souboru,  
do kterého bude Docker nainstalován.

Jakmile je Docker jednou spuštěn, bude se po každém spuštění pole spouštět automaticky.
:end

:docker_vdisk_size_help:
Pokud systém musí vytvořit nový image soubor Dockeru, použije se tato výchozí velikost (v GB).

Chceš-li změnit velikost již existujícího image, zadej zde novou velikost.  
Při příštím spuštění Dockeru se image rozšíří (zmenšení není možné).
:end

:docker_vdisk_location_help:
Musíš zadat cestu k image souboru Dockeru.  
Systém tento soubor automaticky vytvoří při prvním spuštění Docker služby.

Název image souboru musí končit příponou `.img`. Jinak nebude vstup akceptován a bude označen červeně.

Doporučujeme uložit image mimo pole – například do cache poolu.  
Pro nejlepší výkon se doporučuje použití SSD.
:end

:docker_appdata_location_help:
Můžeš zadat složku, do které budou automaticky ukládány konfigurační podsložky pro jednotlivé Docker aplikace (odpovídající připojenému svazku `/config`).

Cesta musí končit lomítkem (/). Pokud ne, vstup nebude akceptován a bude označen červeně.

Doporučuje se použít umístění mimo pole, např. v cache poolu.  
Pro nejlepší výkon použij SSD zařízení.

Toto nastavení se použije pouze při přidávání nových Docker aplikací.  
Změna nemá vliv na již existující aplikace.
:end

:docker_log_rotation_help:
Ve výchozím nastavení je rotace logu vypnutá a generuje se jediný log soubor s neomezenou velikostí.

Aktivací rotace logu můžeš omezit velikost jednotlivých souborů a určit, kolik starších verzí se má uchovávat.
:end

:docker_log_file_size_help:
Určuje maximální velikost jednoho log souboru.  
Po jejím překročení se spustí rotace logu.
:end

:docker_log_file_number_help:
Určuje, kolik log souborů se uchová během rotace logů.
:end

:docker_authoring_mode_help:
Pokud nastavíš **Ano**, při vytváření/upravování kontejnerů se v rozhraní zobrazí další pole pro tvorbu šablon.
:end

:docker_custom_network_access_help:
Povolí přímou komunikaci mezi hostem a kontejnery přes vlastní (macvlan) síť.<br>  
Tato možnost je ve výchozím nastavení vypnutá.
:end

:docker_user_defined_network_help:
Uživatelem vytvořené sítě jsou ty, které byly vytvořeny mimo webové rozhraní.<br>  
Ve výchozím nastavení jsou tyto sítě Dockerem odstraněny, aby se zabránilo konfliktům s automaticky generovanými sítěmi.

Změnou tohoto nastavení je možné vlastní sítě povolit.  
Je však na odpovědnosti uživatele zajistit, že budou fungovat správně a bez konfliktů.
:end

:docker_include_interface_vlan_ipv4_help:
Zahrne (výchozí) nebo vyloučí výše uvedená rozhraní nebo VLAN jako vlastní síť pro Docker.

Zadej rozsah adres (pool) v rámci každé přiřazené podsítě, který bude použit pro DHCPv4 přidělování adres Dockerem. Např. 192.168.1.128/25
:end

:docker_exclude_interface_vlan_ipv4_help:
Zahrne nebo vyloučí vlastní sítě Dockeru (výchozí).

Zadej rozsah adres (pool) v rámci každé přiřazené podsítě, který bude použit pro DHCPv4 přidělování adres Dockerem. Např. 192.168.1.128/25
:end

:docker_include_interface_vlan_ipv6_help:
Zahrne nebo vyloučí vlastní rozhraní a VLAN pro Docker (výchozí je zahrnout).

Zadej rozsah adres (pool) v rámci každé přiřazené podsítě, který bude použit pro DHCPv6 přidělování adres Dockerem. Např. 2a02:abcd:9ef5:100:1::/72
:end

:docker_exclude_interface_vlan_ipv6_help:
Zahrne nebo vyloučí vlastní rozhraní a VLAN pro Docker (výchozí je zahrnout).

Zadej rozsah adres (pool) v rámci každé přiřazené podsítě, který bude použit pro DHCPv6 přidělování adres Dockerem. Např. 2a02:abcd:9ef5:100:1::/72
:end

:docker_version_help:
Toto je aktuálně používaná verze Dockeru.
:end

:docker_vdisk_location_active_help:
Toto je umístění image souboru Dockeru.
:end

:docker_appdata_location_active_help:
Toto je umístění uživatelských souborů pro Docker.
:end

:docker_log_rotation_active_help:
Ve výchozím nastavení je generován log soubor bez omezení velikosti.  
Pokud je aktivní rotace logu, je omezena velikost a počet uchovaných log souborů.
:end

:docker_custom_network_active_help:
Umožňuje přímou komunikaci mezi hostem a kontejnery přes vlastní (Macvlan) síť.<br>  
Ve výchozím nastavení je tato možnost zakázána.
:end

:docker_user_defined_network_active_help:
Zobrazuje, zda se používají sítě vytvořené mimo webové rozhraní Dockeru.  
Pokud ano, budou tyto *vlastní sítě* dostupné v rozbalovací nabídce *Typ sítě*.
:end

:docker_scrub_help:
**Scrub** spustí příkaz *btrfs scrub*, který prověří souborový systém.

Pokud je nutná oprava, zaškrtni možnost *Opravit chyby systému souborů* a spusť scrub znovu.  
Tím umožníš nástroji *btrfs scrub* opravit nalezené chyby.
:end

:docker_cancel_help:
**Zrušit** přeruší probíhající operaci scrub.
:end

:id_server_name_help:
Síťová identita tvého serveru – také známá jako *hostname* nebo *krátké jméno hostitele*.  
V prostředí Windows je označována jako *NetBIOS název* a může mít maximálně 15 znaků.

Používej pouze alfanumerické znaky („A–Z“, „a–z“, „0–9“), pomlčky („-“) a tečky („.“).  
První a poslední znak musí být alfanumerický.
:end

:id_description_help:
Zobrazuje se jako textové pole vedle serveru v Průzkumníku Windows nebo ve Finderu (macOS).
:end

:id_model_help:
Zobrazí modelové číslo serveru.
:end

:mgmt_start_page_help:
Určuje stránku, která se otevře při spuštění webového rozhraní.  
Výchozí stránka je *Main*.
:end

:mgmt_use_telnet_help:
Ve výchozím nastavení je přístup přes TELNET povolen.  
TELNET je však nezabezpečený způsob přístupu přes příkazovou řádku – doporučujeme místo něj používat SSH a TELNET vypnout.
:end

:mgmt_telnet_port_help:
Zadej port pro TELNET připojení. Výchozí hodnota je 23.
:end

:mgmt_use_ssh_help:
SSH je ve výchozím nastavení povoleno a poskytuje bezpečný způsob přístupu přes příkazovou řádku.  
Pokud systém nemá vygenerované SSH klíče, vytvoří je při spuštění serveru a uloží do */config/ssh*.
:end

:mgmt_ssh_port_help:
Zadej port pro SSH připojení. Výchozí hodnota je 22.
:end

:mgmt_use_upnp_help:
Povolí (výchozí) nebo zakáže funkci UPnP na serveru.  
Tato funkce umožňuje automatické přesměrování portů na routeru – musí být povolena i přímo v nastavení routeru.
:end

:mgmt_use_ssl_tls_help:
Určuje, jak bude webové rozhraní reagovat na HTTP a/nebo HTTPS požadavky.

Při nastavení na **Ne** bude HTTPS deaktivováno.

Při nastavení na **Ano** bude HTTPS aktivováno a veškerý HTTP provoz bude přesměrován na HTTPS.  
Pokud není k dispozici SSL certifikát Let's Encrypt, bude automaticky vytvořen a použit self-signed certifikát.

Při nastavení na **Auto** bude použit certifikát Let's Encrypt vydaný službou Lime Technology.  
Dokud nebude certifikát vydán, rozhraní zůstane v režimu HTTP. Jakmile je certifikát dostupný, přepne se rozhraní na HTTPS.  
Dále budou spuštěny dva procesy na pozadí:

- *updatedns* – Spustí se 30 sekund po startu serveru a spojí se s DNS službou Lime Technology, aby zaregistroval IP adresu serveru.  
  Poté běží každých 10 minut a kontroluje případné změny IP adresy.

- *renewcert* – Spustí se 60 sekund po startu serveru a spojí se se službou Lime Technology pro obnovu certifikátu.  
  Certifikát je obnoven, pokud je třeba. Tento proces se opakuje každých 24 hodin.  
  30 dní před vypršením platnosti je automaticky vygenerován a nainstalován nový certifikát.

Poznámka: Po vydání certifikátu Let's Encrypt lze procesy *updatedns* a *renewcert* deaktivovat přepnutím tohoto pole na **Ano**.

**Informace o práci s nginx certifikátem**

Startovací skript nginx hledá SSL certifikát v tomto pořadí na USB flash zařízení:<br>  
`config/ssl/certs/certificate_bundle.pem`<br>  
`config/ssl/certs/<server-name>_unraid_bundle.pem`

Pokud žádný z těchto souborů neexistuje, bude automaticky vytvořen self-signed certifikát a uložen jako:<br>  
`config/ssl/certs/<server-name>_unraid_bundle.pem`<br>

Pokud je použit certifikát Let's Encrypt, bude uložen jako:<br>  
`config/ssl/certs/certificate_bundle.pem`

**Podpora nginx OCSP staplingu**

Podle použitého certifikátu:

`config/ssl/certs/certificate_bundle.pem` => Ano<br>  
`config/ssl/certs/<server-name>_unraid_bundle.pem` => Ne
:end

:mgmt_http_port_help:
Zadej port pro HTTP připojení. Výchozí hodnota je 80.
:end

:mgmt_https_port_help:
Zadej port pro HTTPS připojení. Výchozí hodnota je 443.
:end

:mgmt_local_tld_help:
Zadej svou vlastní top-level doménu (TLD). Může zůstat prázdné.
:end

:mgmt_certificate_expiration_help:
**Vystavit** – použije se pro získání *bezplatného* SSL certifikátu od [Let's Encrypt](https://letsencrypt.org/) a jeho nahrání.  
Poznámka: Důrazně doporučujeme použití statické IP adresy.

**DNS aktualizace** – slouží k ruční aktualizaci DNS A záznamu tvého FQDN na unraid.net.  
Poznámka: Aktualizace může trvat několik minut až hodin (náš TTL je nastaven na 60 sekund).

**Poznámka:** Funkce **Vystavit** nebude fungovat, pokud máš na routeru nebo upstream DNS serveru aktivní  
[ochranu proti DNS rebindingu](https://en.wikipedia.org/wiki/DNS_rebinding).  
Tato ochrana brání rozlišení privátních IP adres přes DNS a používá se kvůli bezpečnosti starších zařízení s nezabezpečeným webovým rozhraním.

Tuto ochranu často aktivují poskytovatelé internetu (ISP) – lze ji obejít např. přechodem na veřejné DNS servery (např. Google DNS).

Obvykle lze ochranu proti DNS rebindingu spravovat přímo v nastavení routeru.  
Většina běžných domácích routerů tuto ochranu nemá. Pokud tvůj router tuto funkci má, deaktivuj ji.

**Příklady konfigurace pro pokročilé routery:**

**DD-WRT:** Pokud používáš "dnsmasq" s ochranou proti DNS rebindingu, přidej do konfigurace:

`rebind-domain-ok=/unraid.net/`

**pfSense:** Pokud používáš interní resolver pfSense, přidej:

`server:`  
`private-domain: "unraid.net"`

**Ubiquiti USG Router:** přidej:

`set service dns forwarding options rebind-domain-ok=/unraid.net/`

**OpenDNS:** V nastavení -> Zabezpečení *odškrtni* volbu  
„Podezřelé odpovědi – blokovat interní IP adresy“. Buď vše blokovat, nebo nic.

Pokud nic z toho nefunguje, můžeš upravit soubor *hosts* na svém počítači a přesměrovat dotazy na FQDN unraid.net přímo na lokální IP adresu serveru.
:end

:ftp_server_help:
Povol nebo zakaž FTP službu na pozadí. FTP server je ve výchozím nastavení povolen.  
Toto nastavení není trvalé – po restartu serveru se vrátí na výchozí hodnotu.
:end

:ftp_users_help:
Zadej uživatelská jména, která mají mít přístup k FTP serveru (oddělená mezerami).  
Chceš-li zakázat přístup všem uživatelům, nech pole prázdné.

**Poznámka:** Pro předejití problémům **nepřidávej** uživatele `root`.
:end

:ftp_overview_help:
### Přehled

Unraid obsahuje oblíbený FTP server `vsftpd`. Konfigurace je aktuálně velmi jednoduchá:  
**Všichni** výše uvedení uživatelé mají přístup k FTP a získají  
*plná práva pro čtení/zápis/smazání* na celém serveru – používej s opatrností.
:end

:smb_enable_help:
Pokud nastavíš **Ano (Pracovní skupina)**, aktivuje se SMB protokol (Windows Networking),  
včetně detekce počítačů ve Windows síti.

Pokud nastavíš **Ano (Active Directory)**, aktivuje se integrace s Active Directory.
:end

:smb_hide_files_help:
Pokud nastavíš **Ano**, budou všechny soubory začínající tečkou (např. `.file`) označeny jako *skryté*  
a ve Windows se nezobrazí – pokud není zapnuto zobrazení skrytých souborů.

Pokud nastavíš **Ne**, budou tyto soubory zobrazovány běžně jako ostatní.
:end

:smb_enhanced_macos_help:
Pokud nastavíš **Ano**, zlepší se kompatibilita SMB pro macOS.  
Například Finder správně zobrazuje sdílené složky a umožňuje export Time Machine záloh.  
Může ale způsobit problémy ve Windows.

Více informací najdeš na [VFS_FRUIT MAN PAGE](https://www.mankier.com/8/vfs_fruit).
:end

:smb_enable_netbios_help:
Pokud nastavíš **Ano**, povolí se NetBIOS. To zároveň aktivuje podporu SMBv1.  
Pokud zakážeš, musí klienti podporovat SMBv2 nebo vyšší.
:end

:smb_enable_wsd_help:
Pokud nastavíš **Ano**, povolí se WSD (WS-Discovery).  
Vypínej pouze pokud používáš síť založenou na SMBv1.
:end

:smb_wsd_options_help:
Toto pole bude přidáno ke spouštěcím možnostem WSD služby.  
Ponech prázdné, pokud ti support nedoporučil jinak.
:end

:smb_extra_conf_help:
Na této stránce můžeš upravit obsah souboru `smb-extra.conf`.  
Změny se projeví až po restartu Samba služby.
:end

:smb_extra_button_help:
Klikni na **Použít**, pro uložení provedených změn.  
Klikni na **Reset**, pro zrušení změn (před uložením).  
Klikni na **Hotovo**, pro návrat zpět.
:end

:smb_workgroup_help:
Nastaví název pracovní skupiny. Obvykle „WORKGROUP“.
:end

:smb_local_master_help:
Pokud nastavíš **Ano**, bude se server účastnit volby lokálního správce (Local Master Browser).  
Pokud nejsou přítomny jiné servery, server tuto roli převezme a bude zasílat aktualizace klientům.
:end

:nfs_enable_help:
Pokud nastavíš **Ano**, aktivuje se NFS protokol.
:end

:nfs_tunable_fuse_remember_help:
Pokud je aktivní NFS, toto nastavení (*fuse_remember*) může pomoci snížit nebo vyřešit chyby „NFS Stale File Handles“,  
které se mohou objevit při přístupu klienta k serveru.

Hodnota určuje, jak dlouho (v sekundách) si systém uchová v paměti názvy souborů a složek.  
Výchozí hodnota je 330 (5,5 minuty). Dále jsou k dispozici tyto hodnoty:

* 0 – žádné názvy nejsou ukládány do paměti  
* -1 – názvy jsou uloženy trvale (dokud není pole zastaveno)

Nastavení 0 je vhodné, pokud NFS používáš pouze pro přístup k jednotlivým diskům, ne pro sdílené složky.

Nastavení -1 může být nutné, pokud žádná jiná hodnota nevyřeší „stale file handle“ chyby.  
Upozornění: Každý název souboru/složky uložený v paměti zabírá cca 108 bajtů RAM.  
To může vést k nedostatku paměti v závislosti na velikosti stromu souborů a velikosti RAM.
:end

:shares_enable_disk_help:
If set to No, disk shares are unconditionally not exported.

If set to Yes, disk shares may be exported.  **WARNING:** Do not copy data from a disk share to a user share
unless you *know* what you are doing.  This may result in the loss of data and is not supported.

If set to Auto, only disk shares not participating in User Shares may be exported.
:end

:shares_enable_shares_help:
If set to 'Yes' the User Shares feature is activated.
:end

:shares_included_disks_help:
This setting defines the set of array disks which are *included* in User Shares.
Unchecking all disks will allow **all** array disks to be included.
:end

:shares_excluded_disks_help:
This setting defines the set of array disk which are *excluded* from User Shares.
Uncheck all disks in order to not exclude any disks

**Note:** Each separate User Share also includes its own set of Included and Excluded
disks which represent a subset of the Included/Excluded disks defined here.
:end

:shares_tunable_hard_links_help:
If set to Yes then support the link() operation.

If set to No then hard links are not supported.

Notes:

* Setting to Yes may cause problems for older media and dvd/bluray players accessing shares using NFS.
* No matter how this is set, the **mover** will still properly handle any detected hard links.
:end

:shares_tunable_direct_io_help:
**Experimental**: If set to Yes then mount User Share file system with FUSE *direct_io* mount option.
This will increase write performance but might possibly decrease read performance.

*Auto* selects No.
:end

:syslog_local_server_help:
Let the server act as a central syslog server and collect syslog messages from other systems.
The server can listen on UDP, TCP or both with a selectable port number.

Syslog information is stored per IP address. That is every system gets its own syslog file.
:end

:syslog_local_folder_help:
Select the share folder where the syslogs will be stored.
It is recommended that you use a share located on the cache drive to prevent array disk spinups.
:end

:syslog_local_rotation_help:
By default LOG rotation is disabled and will create a single LOG file of unlimited size.

Enable LOG rotation to limit the size of the LOG file and specify the number of files to keep in the rotation scheme.
:end

:syslog_local_file_size_help:
Specifies the maximum LOG size. When exceeded LOG rotation will occur.
:end

:syslog_local_file_number_help:
Specifies the number of additional LOG files to keep in the rotation scheme.
:end

:syslog_remote_server_help:
Enter a name or IP address of a remote syslog server.
This will send a copy of the syslog messages to the designated server.
:end

:syslog_mirror_flash_help:
This setting is NO by default and must be used with care to avoid unnecessary wear and tear of the USB device.

Change this setting to YES when troubleshooting is required and it is not possible to get the regular diagnostics information.
A mirror of the syslog file is stored in the **logs** folder of the flash device.
:end

:confirm_reboot_help:
Choose if rebooting or powering down the server needs a confirmation checkbox.
:end

:confirm_array_stop_help:
Choose if stopping the array needs a confirmation checkbox.
:end

:display_settings_help:
The display settings below determine how items are displayed on screen. Use these settings to tweak the visual effects to your likings.

You can experiment with these settings as desired, they only affect visual properties.
:end

:display_font_size_help:
Changes the font size in the GUI. This is a per device setting.
:end

:display_tty_size_help:
Changes the font size of terminal windows.
:end

:display_page_view_help:
Changes how certain pages are displayed. In **Tabbed** mode different sections will be displayed in different tabs,
while in **Non-tabbed** mode sections are displayed under each other.
:end

:display_users_menu_help:
The Users Menu can be part of the header or part of the Settings menu.
You can move the Users Menu if insufficient space in the header is available to display all menus.
:end

:display_listing_height_help:
**Automatic** : long listings are displayed as is, and the user needs to scroll the whole page to see the bottom

**Fixed** : long listings are displayed in a window with a fixed size, user can scroll this window to see the bottom
:end

:display_wwn_device_id_help:
World Wide Name (WWN) is a unique identifier used for SAS attached devices.

Select *Disabled* to suppress the appending of WWN to the device identification

Select "Automatic" to append WWN to the device identification in case of SAS devices
:end

:display_custom_text_color_help:
Overrule the default text color in the header. This can be used to match the text color with a background image.
:end

:display_custom_background_color_help:
Overrule the default background color in the header. This can be used to match the background color with a custom text color.
:end

:display_custom_banner_help:
Image will be scaled to 1920x90 pixels. The maximum image file upload size is 512 kB (524,288 bytes).
:end

:display_temperature_unit_help:
Selects the temperature unit for the disk temperature thresholds. Changing the unit will adjust the existing value in the disk temperature thresholds as appropriate.

Make sure any newly entered values represent the selected temperature unit.
:end

:vms_enable_help:
Stopping the VM Manager will first attempt to shutdown all running VMs. After 60 seconds, any remaining VM instances will be terminated.
:end

:vms_libvirt_volume_help:
This is the libvirt volume.
:end

:vms_libvirt_vdisk_size_help:
If the system needs to create a new libvirt image file, this is the default size to use specified in GB.
To resize an existing image file, specify the new size here. Next time the Libvirt service is started the file (and file system) will be increased to the new size (but never decreased).
:end

:vms_libvirt_location_help:
You must specify an image file for Libvirt. The system will automatically create this file when the Libvirt service is first started.
:end

:vms_libvirt_storage_help:
Specify a user share that contains all your VM subdirectories with vdisks
:end

:vms_libvirt_iso_storage_help:
Specify a user share that contains all your installation media for operating systems
:end

:vms_virtio_driver_help:
Specify the virtual CD-ROM (ISO) that contains the VirtIO Windows drivers as provided by the Fedora Project.
Download the latest ISO from here: <a href="https://docs.fedoraproject.org/en-US/quick-docs/creating-windows-virtual-machines-using-virtio-drivers/index.html#virtio-win-direct-downloads" target="_blank">fedoraproject.org</a>

When installing Windows, you will reach a step where no disk devices will be found. There is an option to browse for drivers on that screen.
Click browse and locate the additional CD-ROM in the menu. Inside there will be various folders for the different versions of Windows.
Open the folder for the version of Windows you are installing and then select the AMD64 subfolder inside (even if you are on an Intel system, select AMD64).
Three drivers will be found. Select them all, click next, and the vDisks you have assigned will appear.
:end

:vms_network_source_help:
Select the name of the network you wish to use as default for your VMs.
You can choose between **'bridges'** created under network settings or
**'libvirt'** created with virsh command in the terminal.
The bridge **'virbr0'** and the associated virtual network **'default'** are
created by libvirt.  
Both utilizes NAT (network address translation) and act as a DHCP server to hand out IP addresses to virtual machines directly.
More optional selections are present for bridges under network settings or for libvirt networks with the virsh command in the terminal.

**If your are unsure, choose 'virbr0' as the recommended Unraid default.**

NOTE: You can also specify a network source on a per-VM basis.

**IMPORTANT: Neither Libvirt nor Unraid automatically brings up an interface that is assigned to a Libvirt network.
Before you use a Libvirt network, please go to Settings -> Network Settings and, if necessary, manually set the associated interface to up.**
:end

:vms_host_shutdown_help:
When shutting down the server, this defines the action to take upon running VMs. If *Hibernate VMs* is chosen,
the VM will be instructed to hibernate (if supported) otherwise it will attempt a VM shutdown.
:end

:vms_shutdown_timeout_help:
When shutting down the server, this defines how long to wait in seconds for *graceful* VM shutdown before forcing shutdown to continue.
NOTE: It's recommended to shut down guest VMs from within the VM.
:end

:vms_acs_override_help:
*PCIe ACS override* allows various hardware components to expose themselves as isolated devices.
Typically it is sufficient to isolate *Downstream* ports.
A hardware component may need the setting *Multi-function* or *Both* to further isolate different hardware functions.<br>
A reboot is required for changes to this setting to take affect.

**Warning: use of this setting could cause possible data corruption with certain hardware configurations.**
Please visit the [Lime Technology forums](https://forums.unraid.net/forum/51-vm-engine-kvm) for more information.
:end

:vms_vfio_interupts_help:
If your system doesn't support interrupt remapping, these can be enabled by allowing unsafe interrupts.<br>
A reboot will be required for changes to this setting to take affect.

**Warning: use of this setting could cause possible data corruption with certain hardware configurations.**
Please visit the [Lime Technology forums](https://forums.unraid.net/forum/51-vm-engine-kvm) for more information.
:end

:vms_libvirt_log_help:
View the log for libvirt: <a onclick="openTerminal('log','libvirt','libvirt/libvirtd.log')">View libvirtd.log</a>
:end

:vms_scrub_help:
**Scrub** runs the *btrfs scrub* program to check file system integrity.
If repair is needed you should check the *Correct file system errors* and run a second Scrub pass; this will permit *btrfs scrub* to fix the file system.
:end

:vms_cancel_help:
**Cancel* will cancel the Scrub operation in progress.
:end

:eth_interface_description_help:
Use this optional field to provide additional information about the purpose of the connection.
:end

:eth_mac_address_help:
This is the hardware address of the interface.
When tagging is enabled all VLANs on this interface will share the same hardware address.
:end

:eth_enable_bonding_help:
Bonding is a feature that combines multiple physical Ethernet interfaces into a single *bonded* interface named **bond0**.
This can be used to improve the connection redundancy and/or throughput of the system.
Different bonding modes are supported (see below), but some modes require proper switch support.
:end

:eth_bonding_mode_help:
**Mode 0 (balance-rr)**<br>
This mode transmits packets in a sequential order from the first available slave through the last.
If two real interfaces are slaves in the bond and two packets arrive destined out of the bonded interface the first will be transmitted on the first slave and the second frame will be transmitted on the second slave.
The third packet will be sent on the first and so on. This provides load balancing and fault tolerance.

**Mode 1 (active-backup) - default**<br>
This mode places one of the interfaces into a backup state and will only make it active if the link is lost by the active interface.
Only one slave in the bond is active at an instance of time. A different slave becomes active only when the active slave fails.
This mode provides fault tolerance.

**Mode 2 (balance-xor)**<br>
This mode transmits packets based on an XOR formula. Source MAC address is XOR'd with destination MAC address modula slave count.
This selects the same slave for each destination MAC address and provides load balancing and fault tolerance.

**Mode 3 (broadcast)**<br>
This mode transmits everything on all slave interfaces. This mode is least used (only for specific purpose) and provides only fault tolerance.

**Mode 4 (802.3ad)**<br>
This mode is known as *Dynamic Link Aggregation*. It creates aggregation groups that share the same speed and duplex settings.
It requires a switch that supports IEEE 802.3ad dynamic link.
Slave selection for outgoing traffic is done according to the transmit hash policy, which may be changed from the default simple XOR policy via the xmit_hash_policy option.
Note that not all transmit policies may be 802.3ad compliant, particularly in regards to the packet mis-ordering requirements of section 43.2.4 of the 802.3ad standard.
Different peer implementations will have varying tolerances for noncompliance.

**Mode 5 (balance-tlb)**<br>
This mode is called *Adaptive transmit load balancing*. The outgoing traffic is distributed according to the current load and queue on each slave interface.
Incoming traffic is received by the current slave.

**Mode 6 (balance-alb)**<br>
This mode is called *Adaptive load balancing*. This includes balance-tlb + receive load balancing (rlb) for IPV4 traffic.
The receive load balancing is achieved by ARP negotiation.
The bonding driver intercepts the ARP Replies sent by the server on their way out and overwrites the src hw address with the unique hw address of one of the slaves in the bond
such that different clients use different hw addresses for the server.

*Mode 1 (active-backup) is the recommended setting. Other modes allow you to set up a specific environment, but may require proper switch support.
Choosing a unsupported mode can result in a disrupted communication.*
:end

:eth_bonding_members_help:
Select which interfaces are member of the *bonded* interface. By default eth0 is a member, while other interfaces are optional.
:end

:eth_enable_bridging_help:
Bridging is a feature which creates a virtual bridge and allows VMs and Docker containers to communicate directly with the physical Ethernet port.
Both bonding and bridging can be combined to let VMs or containers communicate over a *bonded* interface.
:end

:eth_bridging_members_help:
Select which interfaces are member of the *bridged* interface. By default eth0 is a member, while other interfaces are optional.
:end

:eth_network_protocol_help:
Select which protocol(s) are used. By default IPv4 only is used.<br>
When both IPv4 and IPv6 is selected, each protocol can be configured independently.
:end

:eth_ipv4_address_assignment_help:
The following settings are possible:

*Automatic* - the server will attempt to obtain a IPv4 address from the local DHCP server<br>
*Static* - the IPv4 address is manually set for this interface<br>
*None* - no IPv4 address is assigned to the interface (only available for VLANs)
:end

:eth_ipv4_address_help:
Greyed out when using automatic IP assignment. Otherwise specify here the IPv4 address and mask of the system.
:end

:eth_ipv4_default_gateway_help:
Greyed out when using automatic IP assignment. Otherwise specify here the IPv4 address of your router.
:end

:eth_ipv4_dns_server_assignment_help:
If set to *Automatic* the server will use IPv4 DNS server(s) returned by the local automatic assignment.<br>
If set to *Static* you may enter your own list.

This is useful in Active Directory configurations where you need to set the first DNS Server entry to the IP address of your AD Domain server.
:end

:eth_ipv4_dns_server_help:
This is the primary IPv4 DNS server to use. Enter a IPv4 address.

Note: for *Active Directory* you **must** ensure this is set to the IP address of your AD Domain server.
:end

:eth_ipv4_dns_server2_help:
This is the IPv4 DNS server to use when IPv4 DNS server 1 is down.
:end

:eth_ipv4_dns_server3_help:
This is the IPv4 DNS server to use when IPv4 DNS servers 1 and 2 are both down.
:end

:eth_ipv6_address_assignment_help:
The following settings are possible:

*Automatic* - the server will attempt to obtain a IPv6 address from the local DHCP server or Router Advertisement (RA)<br>
*Static* - the IPv6 address is manually set for this interface<br>
*None* - no IPv6 address is assigned to the interface (only available for VLANs)
:end

:eth_ipv6_address_help:
Greyed out when using automatic IP assignment. Otherwise specify here the IPv6 address of the system.
:end

:eth_ipv6_default_gateway_help:
Greyed out when using automatic IP assignment. Otherwise specify here the IPv6 address of your router.
:end

:eth_ipv6_privacy_extensions_help:
Enable or disable the generation of a random IPv6 interface identifier according to RFC4941. This is similar to the temporary IPv6 address generation on Windows machines.
:end

:eth_ipv6_dns_server_assignment_help:
If set to *Automatic* the server will use IPv6 DNS server(s) returned by the local automatic assignment.<br>
If set to *Static* you may enter your own list.
:end

:eth_ipv6_dns_server_help:
This is the primary IPv6 DNS server to use. Enter a IPv6 address.
:end

:eth_ipv6_dns_server2_help:
This is the IPv6 DNS server to use when IPv6 DNS server 1 is down.
:end

:eth_ipv6_dns_server3_help:
This is the IPv6 DNS server to use when IPv6 DNS servers 1 and 2 are both down.
:end

:eth_desired_mtu_help:
This is the MTU size to use on the physical Ethernet interface.
If left blank, the MTU will automatically be determined (by default 1500 bytes).
:end

:eth_enable_vlans_help:
By default no VLANs are configured.<br>
Enabling VLANs extends the number of logical connections over the same physical connection.

Note: your router and switch must support VLANs too when this feature is used.
:end

:eth_vlan_number_help:
Give each VLAN a unique identifier. Numbers range from 1 to 4095.
:end

:eth_network_rules_help:
The interface assignment rules can be changed here and might be necessary to set the preferred interface for managing Unraid - *use with care, usually there is no need to change*.

**eth0** is the main interface used to manage the Unraid system. The other interfaces are optional and may be used as desired.
Every interface must be uniquely identified by its MAC (hardware) address.

The interface assignment is stored on the flash device under */config/network-rules.cfg*. This file can be viewed with any editor, but it is recommended to make changes via the webGUI only.<br>
Deleting the file *network-rules.cfg* from the flash device will restore automatic interface assignment after a system reboot.
:end

:eth_routing_table_help:
Enter a valid IPv4 route in the format *nnn.nnn.nnn.nnn/xx*, e.g. *192.168.1.0/24*<br>
or enter a valid IPv6 route in the format *nnnn:nnnn:nnnn::nnnn/xxx*, e.g. *fe80::3ad8:2fff:fe25:9709/64*

Select the gateway from the dropdown list or enter a valid IPv4/IPv6 address as gateway value.

The metric value is optional, it defaults to 1. Use it to select the preferred gateway when more than one entry of the same route exists.
:end

:apc_ups_daemon_help:
Set to 'Yes' to enable apcupsd and start the daemon, set to 'No' to disable apcupsd and stop the daemon.
:end

:apc_ups_cable_help:
Defines the type of cable connecting the UPS to your computer.Possible generic choices for 'cable' are:

+ USB, Simple, Smart, Ether, or Custom to specify a special cable.
:end

:apc_ups_custom_cable_help:
Specify a special cable by model number, only applicable when *UPS cable* is set to Custom.

+ 940-0119A, 940-0127A, 940-0128A, 940-0020B
+ 940-0020C, 940-0023A, 940-0024B, 940-0024C
+ 940-1524C, 940-0024G, 940-0095A, 940-0095B
+ 940-0095C, 940-0625A, M-04-02-2000
:end

:apc_ups_type_help:
Define a *UPS type*, which corresponds to the type of UPS you have (see the Description for more details).

+ **USB** - most new UPSes are USB
+ **APCsmart** - newer serial character device, appropriate for SmartUPS models using a serial cable (not USB)
+ **Net** - network link to a master apcupsd through apcupsd's Network Information Server. This is used if the UPS powering your computer is connected to a different computer for monitoring
+ **SNMP** - SNMP network link to an SNMP-enabled UPS device
+ **Dumb** - old serial character device for use with simple-signaling UPSes
+ **PCnet** - PowerChute Network Shutdown protocol which can be used as an alternative to SNMP with the AP9617 family of smart slot cards
+ **ModBus** - serial device for use with newest SmartUPS models supporting the MODBUS protocol
:end

:apc_ups_device_help:
Enter the *device* which corresponds to your situation, only applicable when *UPS type* is not set to USB.

+ **apcsmart** - /dev/tty**
+ **net** - hostname:port. Hostname is the IP address of the NIS server. The default port is 3551
+ **snmp** - hostname:port:vendor:community. Hostname is the ip address or hostname of the UPS on the network. Vendor can be can be "APC" or "APC_NOTRAP". "APC_NOTRAP" will disable SNMP trap catching; you usually want "APC". Port is usually 161. Community is usually "private"
+ **dumb** - /dev/tty**
+ **pcnet** - ipaddr:username:passphrase:port. ipaddr is the IP address of the UPS management card. username and passphrase are the credentials for which the card has been configured. port is the port number on which to listen for messages from the UPS, normally 3052. If this parameter is empty or missing, the default of 3052 will be used
+ **modbus** - /dev/tty**
:end

:apc_battery_level_help:
If during a power failure, the remaining battery percentage (as reported by the UPS) is below or equal to *Battery level*, apcupsd will initiate a system shutdown.
:end

:apc_runtime_left_help:
If during a power failure, the remaining runtime in minutes (as calculated internally by the UPS) is below or equal to this field, apcupsd, will initiate a system shutdown.
:end

:apc_battery_time_help:
If during a power failure, the UPS has run on batteries for *time-out* many seconds or longer; apcupsd will initiate a system shutdown. A value of zero disables this timer.

If you have a Smart UPS, you will most likely want to disable this timer by setting it to zero.
That way, your UPS will continue on batteries until either the % charge remaining drops to or below *Battery level* or the remaining battery runtime drops to or below *minutes*.

Of course - when testing - setting this to 60 causes a quick system shutdown if you pull the power plug.
If you have an older dumb UPS, you will want to set this to less than the time you know you can run on batteries.
:end

:apc_note_help:
**Note:** *Battery level*, *Runtime left*, and *Time on battery* work in conjunction, so the first that occurs will cause the initiation of a shutdown.
:end

:apc_killups_help:
Set to *Yes* to turn off the power to the UPS after a shutdown.
:end

:parity_check_scheduled_help:
By default no parity check is scheduled. Select here the desired schedule. This can be one of the preset schedules for daily, weekly, monthly, yearly or a custom schedule.
:end

:parity_day_of_the_week_help:
When a **weekly** or **custom** schedule is selected then choose here the preferred *day of the week*, in the other schedules this setting is not used and unavailable.
:end

:parity_week_of_the_month_help:
When a **monthly** or **yearly** schedule is selected then choose here the preferred *day of the month*.
When a **custom** schedule is selected then choose here the preferred *week of the month*, in the other schedules this setting is not used and unavailable.
:end

:parity_time_of_the_day_help:
Choose the desired *time of the day* to start the schedule. Time granularity is given in half hour periods.
:end

:parity_month_of_the_year_help:
When a **yearly** or **custom** schedule is selected then choose here the preferred *month of the year*, in the other schedules this setting is not used and unavailable.
:end

:parity_write_corrections_help:
Choose here whether any parity errors found during the check, need to be corrected on the Parity disk or not.
:end

:parity_cumulative_check_help:
Change this setting to **Yes** to divide long duration parity checks over multiple periods. This is useful when the system needs to be available and a parity check runs only during off hours.
:end

:parity_accumulation_frequency_help:
Specifies how accumulation periods are executed.

**Daily** means every subsequent day the parity check continues until finished

**Weekly** means every subsequent week the parity check continues until finished
:end

:parity_accumulation_duration_help:
Specifies how long each accumulated period runs, expressed in hours. The **Time of the day** speficies the start time of the period.
:end

:mover_schedule_help:
Choose a mover schedule ranging from hourly, daily, weekly and monthly.

The interval determines how fast the mover will activated, it runs in the background.
:end

:mover_day_of_the_week_help:
Choose a day when the weekly schedule is selected. Otherwise disabled.
:end

:mover_day_of_the_month_help:
Choose a date when the monthly schedule is selected. Otherwise disabled.
:end

:mover_time_of_the_day_help:
When an hourly schedule is selected this will set the interval in hours. An interval always starts on the whole hour (minute 0).

For the other schedules choose here the time of the day the mover should start.
:end

:mover_logging_help:
Write mover messages to the syslog file.
:end

:notifications_display_help:
In *Detailed* view all notifications will be displayed on screen as soon as they arrive.<br>
Notifications can be acknowledged individually or all in once.

In *Summarized* view notifications will be counted only and the number of unread notifications is shown in the menu header per category.<br>
Click on the counters to either acknowledge or view the unread notifications.
:end

:notifications_date_format_help:
Select the desired date format which is used in the notifications archive. Recommended is YYYY-MM-DD, which makes the date/time column sortable in a sensible way.
:end

:notifications_time_format_help:
Select the desired time format which is used in the notifications archive. Recommended is 24 hours, which makes the date/time column sortable in a sensible way.
:end

:notifications_display_position_help:
Choose the position of where notifications appear on screen. Multiple notifications are stacked, bottom-to-top or top-to-bottom depending on the selected placement.
:end

:notifications_store_flash_help:
By default notifications are stored on RAM disk, which will get lost upon system reboot.
Notifications may be stored permanently on the flash drive under folder '/boot/config/plugins/dynamix' instead.
:end

:notifications_system_help:
By default the notifications system is disabled. Enable it here to start receiving notifications.
The following sections give more options about which and what type of notifications will be sent.
:end

:notifications_os_update_help:
Start a periodic verification and notify the user when a new version of the Unraid OS system is detected.
Use the checkboxes below to select how notifications need to be given; by browser, by email and/or by custom agent.
:end

:notifications_plugins_update_help:
Start a periodic verification and notify the user when a new version of one or more of the installed plugins is detected.
Use the checkboxes below to select how notifications need to be given; by browser, by email and/or by custom agent.
:end

:notifications_docker_update_help:
Start a periodic verification and notify the user when a new version of one or more of the installed dockers is detected.
Use the checkboxes below to select how notifications need to be given; by browser, by email and/or by custom agent.
:end

:notifications_array_status_help:
Start a periodic array health check (preventive maintenance) and notify the user the result of this check.
:end

:notifications_agent_selection_help:
Use the checkboxes above to select what and how notifications need to be given; by browser, by email and/or by a service.<br>
<b>Tip:</b> you can use custom notification agents; just add them to "/boot/config/plugins/dynamix/notification/agents" directory and check 'Agents'.
:end

:notifications_classification_help:
Notifications are classified as:

*notice* - these are informative notifications and do not indicate a problem situation, e.g. a new version is available<br>
*warning* - these are attentive notifications and may indicate future problems, e.g. a hard disk is hotter than usual<br>
*alert* - these are serious notifications and require immediate attention, e.g. a failing hard disk<br>

Choose for each classification how you want to be notified.
:end

:smtp_preset_service_help:
Select a preset service to set the basic service settings.
:end

:smtp_email_address_help:
Email address of your mail account. This address is used as sender of the notifications.
:end

:smtp_recipients_help:
Recipients of status and error notifications. Specify one or more email addresses, separate multiple email addresses with a space.
:end

:smtp_priority_help:
Set email header with high importance, when there is a problem detected by Unraid OS.
:end

:smtp_subject_prefix_help:
Set a prefix for easy recognition of Unraid OS messages.
:end

:smtp_mail_server_help:
Specify the name of the email server. Use the preset service selection to have this filled-in automatically.
:end

:smtp_mail_server_port_help:
Specify the port of the email server. Use the preset service selection to have this filled-in automatically.
:end

:smtp_use_ssl_tls_help:
Specifies whether to use SSL/TLS to talk to the SMTP server.
:end

:smtp_use_starttls_help:
Specifies whether to use STARTTLS before starting SSL negotiation - See RFC 2487.
:end

:smtp_define_tls_cert_help:
Select only when you have a certificate which required for communication.
:end

:smtp_tls_cert_location_help:
The file name of an RSA certificate to use for TLS - as required.
:end

:smtp_authentication_method_help:
Select the correct authentication method for your email server. Use test to verify that access is working properly.
:end

:smtp_username_password_help:
Enter the username and password to login to your email account. Be aware that the password is stored unencrypted in the email configuration file.
:end

:plugin_install_help:
To download and install a plugin, enter the plg file URL and click **Install**. A window will open
that displays install progress. Do not close this window until install has completed. You may also specify
the local file name of an extension.
:end

:plugin_error_help:
These plugins were not installed because of some kind of installation error.  You should delete these
plugins and then **reboot** your server.*
:end

:plugin_stale_help:
These plugins were not installed because newer code already exists.  It is safe to simply delete these.
:end

:docker_client_general_help:
Templates are a quicker way to setting up Docker Containers on your Unraid server.  There are two types of templates:

**Default templates**<br>
When valid repositories are added to your Docker Repositories page, they will appear in a section on this drop down for you to choose (master categorized by author, then by application template).
After selecting a default template, the page will populate with new information about the application in the Description field, and will typically provide instructions for how to setup the container.
Select a default template when it is the first time you are configuring this application.

**User-defined templates**<br>
Once you've added an application to your system through a Default template,
the settings you specified are saved to your USB flash device to make it easy to rebuild your applications in the event an upgrade were to fail or if another issue occurred.
To rebuild, simply select the previously loaded application from the User-defined list and all the settings for the container will appear populated from your previous setup.
Clicking create will redownload the necessary files for the application and should restore you to a working state.
To delete a User-defined template, select it from the list above and click the red X to the right of it.
:end

:docker_client_name_help:
Give the container a name or leave it as default.  Two characters minimum.  First character must be a-z A-Z 0-9  Remaining characters a-z A-Z 0-9 . - _
:end

:docker_client_overview_help:
A description for the application container.  Supports basic HTML mark-up.
:end

:docker_client_additional_requirements_help:
Any additional requirements the container has.  Supports basic HTML mark-up.
:end

:docker_client_repository_help:
The repository for the application on the Docker Registry.  Format of authorname/appname.
Optionally you can add a : after appname and request a specific version for the container image.
:end

:docker_client_support_thread_help:
Link to a support thread on Lime-Technology's forum.
:end

:docker_client_project_page_help:
Link to the project page (eg: www.plex.tv)
:end

:docker_client_readme_help:
Link to a readme file or page
:end

:docker_client_hub_url_help:
The path to the container's repository location on the Docker Hub.
:end

:docker_client_template_url_help:
This URL is used to keep the template updated.
:end

:docker_client_icon_url_help:
Link to the icon image for your application (only displayed on dashboard if Show Dashboard apps under Display Settings is set to Icons).
:end

:docker_client_webui_help:
When you click on an application icon from the Docker Containers page, the WebUI option will link to the path in this field.
Use [IP] to identify the IP of your host and [PORT:####] replacing the #'s for your port.
:end

:docker_extra_parameters_help:
If you wish to append additional commands to your Docker container at run-time, you can specify them here.<br>
For all possible Docker run-time commands, see here: <a href="https://docs.docker.com/reference/run/" target="_blank">https://docs.docker.com/reference/run/</a>
:end

:docker_post_arguments_help:
If you wish to append additional arguments AFTER the container definition, you can specify them here.
The content of this field is container specific.
:end

:docker_cpu_pinning_help:
Checking a CPU core(s) will limit the container to run on the selected cores only. Selecting no cores lets the container run on all available cores (default)
:end

:docker_fixed_ip_help:
If the Bridge type is selected, the application’s network access will be restricted to only communicating on the ports specified in the port mappings section.
If the Host type is selected, the application will be given access to communicate using any port on the host that isn’t already mapped to another in-use application/service.
Generally speaking, it is recommended to leave this setting to its default value as specified per application template.

IMPORTANT NOTE:  If adjusting port mappings, do not modify the settings for the Container port as only the Host port can be adjusted.
:end

:docker_privileged_help:
For containers that require the use of host-device access directly or need full exposure to host capabilities, this option will need to be selected.
For more information, see this link: <a href="https://docs.docker.com/engine/reference/run/#runtime-privilege-and-linux-capabilities" target="_blank">https://docs.docker.com/engine/reference/run/#runtime-privilege-and-linux-capabilities</a>
:end

; sysdevs help - added May 18, 2020
:sysdevs_iommu_groups_help:
This displays a list of IOMMU groups available on your system along with the output of the `lspci` command for each IOMMU group.  The numeric identifiers are used to configure PCI pass-through.

Devices you select will be bound to the vfio-pci driver at boot, which makes them available for assignment to a virtual machine, and also prevents the Linux kernel from automatically binding them to any present host driver.

**Note that selecting a device will bind not only the specified device(s), but *all* other devices in the same IOMMU group as well.**

&nbsp;<i class="fa fa-circle orb green-orb middle"></i>&nbsp;This symbol indicates the device is currently bound to the vfio-pci driver.

&nbsp;<i class="fa fa-retweet grey-orb middle"></i>&nbsp;&nbsp;&nbsp;This symbol indicates the device supports FLR (Function Level Reset).

<input type="checkbox" value="" disabled>&nbsp;&nbsp;If a checkbox is greyed out it means the device is in use by Unraid OS and can not be passed through.
:end

:sysdevs_thread_pairings_help:
This displays a list of CPU thread pairings.
:end

:sysdevs_usb_devices_help:
This displays the output of the `lsusb` command.  The numeric identifiers are used to configure PCI pass-through.
:end

:sysdevs_scsi_devices_help:
This displays the output of the `lsscsi` command.  The numeric identifiers are used to configure PCI pass-through.

Note that linux groups ATA, SATA and SAS devices with true SCSI devices.
:end

; unraid.net help - added March 11, 2021
:unraidnet_wanpanel_help:
WAN Port is the external TCP port number setup on your router to NAT/Port Forward traffic from the internet to this
Unraid server SSL port for secure web traffic.
:end

:unraidnet_inactivespanel_help:
Click Activate to set up a local git repo for your local USB Flash boot device and connect to a dedicated remote on unraid.net tied to this server.
:end

:unraidnet_changespanel_help:
The Not Up-to-date status indicates there are local files which are changed vs. the remote on unraid.net.

Click Update to push changes to the remote.

Click Changes to see what has changed.
:end

:unraidnet_uptodatepanel_help:
The Up-to-date status indicates your local configuration matches that stored on the unraid.net remote.
:end

:unraidnet_activepanel_help:
Click Deactivate to pause communication with your remote on unraid.net.

Click Reinitialize to erase all change history in both local and unraid.net remote.
:end

:unraidnet_extraorigins_help:
Provide a comma separated list of urls that are allowed to access the unraid-api (https://abc.myreverseproxy.com,https://xyz.rvrsprx.com,…)
:end

:myservers_remote_t2fa_help:
When Transparent 2FA for Remote Access is enabled, you will access your server by clicking the "Remote Access" link on the <a href="https://forums.unraid.net/my-servers/" target="_blank" rel="noopener noreferrer">My Servers dashboard</a>. The system will transparently request a 2FA token from your server and embed it in the server's login form. Your server will deny any Remote Access login attempt that does not include a valid token. Each token can be used only once, and is only valid for five minutes.
:end

:myservers_local_t2fa_help:
When Transparent 2FA for Local Access is enabled, you will access your server by clicking the "Local Access" link on the <a href="https://forums.unraid.net/my-servers/" target="_blank" rel="noopener noreferrer">My Servers dashboard</a>. The system will transparently request a 2FA token from your server and embed it in the server's login form. Your server will deny any Local Access login attempt that does not include a valid token. Each token can be used only once, and is only valid for five minutes.

This is fairly extreme for Local Access, and in most cases is not needed. It requires a solid Internet connection. If you need to access the webGUI while the Internet is down, SSH to the server and run 'use_ssl no', this will give you access via http://<?=$nginx['NGINX_LANIP']?><?=$nginx['NGINX_PORT'] !== '80' ? ':'.$nginx['NGINX_PORT'] : '' ?>
:end

; WireGuard help text

:wg_local_name_help:
Use this field to set a name for this connection and make it easily recognizable. The same name will appear in the configuration of any peers.
:end

:wg_generate_keypair_help:
Use the **Generate Keypair** button to automatically create a uniqe private and public key combination.<br>
Or paste in an existing private key, generated by WireGuard. Do **NOT** share this private key with others!
:end

:wg_local_tunnel_network_pool_help:
WireGuard tunnels need an internal IP address. Assign a network pool using the default IPv4 network <?=$netpool['wg0']?>/24
or the default IPv6 network <?=$netpool6['wg0']?>/64 or assign your own network pool from which automatic assignment can be done for both this server and any peers.

The *tunnel network pool* must be a unique network not already existing on the server or any of the peers.
:end

:wg_local_tunnel_network_pool_X_help:
WireGuard tunnels need an internal IP address. Assign a network pool using the default IPv4 network <?=$netpool['wgX']?>/24 or
the default IPv6 network <?=$netpool6['wgX']?>/64 or assign your own network pool from which automatic assignment can be done for both this server and any peers.

The *tunnel network pool* must be a unique network not already existing on the server or any of the peers.
:end

:wg_local_tunnel_address_help:
This field is auto filled-in when a local tunnel network pool is created. It is allowed to overwrite the assignment, but this is normally not necessary. Use with care when changing manually.
:end

:wg_local_tunnel_address_help:
This field is auto filled-in when a local tunnel network pool is created. It is allowed to overwrite the assignment, but this is normally not necessary. Use with care when changing manually.
:end

:wg_local_endpoint_help:
This field is automatically filled in with the public management domain name *www.&lt;hash&gt;.myunraid.net* or the public address of the server.<br>
This allows VPN tunnels to be established from external peers to the server.<br>
Configure the correct port forwarding on your router (default port is <?=$netport['wg0']?> but this may be changed) to allow any incoming connections to reach the server.

Users with a registered domain name can use this field to specify how their server is known on the Internet. E.g. www.myserver.mydomain.<br>
Again make sure your router is properly set up.

Note to Cloudflare users: the Cloudflare proxy is designed for http traffic, it is not able to proxy VPN traffic. You must disable the Cloudflare proxy in order to use VPN with your domain.
:end

:wg_local_endpoint_X_help:
This field is automatically filled in with the public management domain name *www.&lt;hash&gt;.myunraid.net* or the public address of the server.<br>
This allows VPN tunnels to be established from external peers to the server.<br>
Configure the correct port forwarding on your router (default port is <?=$netport['wgX']?> but this may be changed) to allow any incoming connections to reach the server.

Users with a registered domain name can use this field to specify how their server is known on the Internet. E.g. www.myserver.mydomain.<br>
Again make sure your router is properly set up.

Note to Cloudflare users: the Cloudflare proxy is designed for http traffic, it is not able to proxy VPN traffic. You must disable the Cloudflare proxy in order to use VPN with your domain.
:end

:wg_local_server_uses_nat_help:
When NAT is enabled, the server uses its own LAN address when forwarding traffic from the tunnel to other devices in the LAN network.
Use this setting when no router modifications are desired, but this approach doesn't work with Docker containers using custom IP addressess.

When NAT is disabled, the server uses the WireGuard tunnel address when forwarding traffic.
In this case it is required that the default gateway (router) has a static route configured to refer tunnel address back to the server.
:end

:wg_local_gateway_uses_upnp_help:
Defaults to YES if the local gateway has UPnP enabled and is responding to requests.<br>
When UPnP is enabled, it is not necessary to configure port forwarding on the router to allow incoming tunnel connections. This is done automatically.
:end

:wg_local_tunnel_firewall_help:
The firewall function controls remote access over the WireGuard tunnel to specific hosts and/or subnets.<br>
The default rule is "deny" and blocks addresses specified in this field, while allowing all others.<br>
Changing the rule to "allow" inverts the selection, meaning only the specified addresses are allowed and all others are blocked.<br>
Use a comma as separator when more than one IP address is entered.
:end

:wg_mtu_size_help:
Leave this to the default automatic mode to select the MTU size. This MTU size is common for all peer connections.
:end

:wg_peer_configuration_help:
The <i class='fa fa-fw fa-eye'></i> icon is used to view a peer's configuration. A configuration can be downloaded or read directly for instant set up of the peer.<br>
The icon is disabled when no peer configuration exists or the user has made changes to the existing settings which are not applied yet.

The <i class='fa fa-fw fa-key'></i> icon is used to show or hide the private, public and preshared keys. Note that these fields are always shown when no keys are set.
:end

:wg_peer_name_help:
Use this field to set a name for this peer connection and make it easily recognizable. The same name will appear in the configuration of the peer at the opposite side.
:end

:wg_peer_preshared_key_help:
For added security a preshared key can be used. Use the **Generate Key** button to automatically create a unique preshared key.<br>
This key is the same at both server and peer side and is added to the peer configuration as well.
:end

:wg_peer_tunnel_address_help:
This field is auto filled-in when a local tunnel network pool is created. It is allowed to overwrite the assignment, but this is normally not necessary. Use with care when changing manually.<br>
Each peer must have a unique tunnel IP address.
:end

:wg_peer_endpoint_help:
When this field is left empty, the server operates in *passive mode* to establish the tunnel. It must be the peer which starts the tunnel.

When an IP address is entered to connect to the peer, the server operates in *active mode* and establishes the tunnel to the peer as soon as there is data to send.

*Note: this field is mandatory for "server-to-server" and "LAN-to-LAN" connections*
:end

:wg_peer_allowed_ips_help:
This field is automatically filled in with the tunnel address of the peer. This allows the server to reach the peer over the tunnel.<br>
When the peer is another server or router with additional networks, then their subnets can be added here to make these networks reachable over the tunnel.
:end

:wg_peer_dns_server_help:
Use this entry to overwrite the current DNS server assignment of the Peer
:end

:wg_persistent_keepalive_help:
By default a WireGuard tunnel stays silent when no traffic is present, which may cause the connection to drop.
Normally this isn't a problem since a WireGuard tunnel is automatically re-established when it is needed.<br>
A keepalive timer will hold the connection open, for most situations a timer value of 20 seconds is suitable.

Note that for mobile devices this will use more data and drain your battery faster.
:end
